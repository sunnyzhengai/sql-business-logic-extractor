# Architecture

This document is the **first thing to read** when navigating this
repository cold. It explains what each top-level directory is for, how
they fit together, and where to look for any given concern.

For the pipeline phase-by-phase guide, see `tools/PHASES.md`.

---

## What this repo is

A toolkit for **mining business logic and surfacing variance from
existing SQL** in healthcare BI environments.

The end goal: catalyze data governance by giving stewards and BI
developers a structured view of how the same concepts are defined
differently across reports, so they can ratify a single source of
truth and move toward certified data models in Fabric.

The pipeline ingests a corpus of SQL views (typically exported from
SSMS) and produces evidence packs for steward conversations:

```
SQL views  →  parse  →  unified graph  →  community detection  →
   steward-ready artifacts  →  human ratification  →  certified definitions
```

This is the substrate for the destination architecture:
**Govern → Model → Self-serve → AI-assisted self-serve.**

---

## Top-level layout

| Directory             | Purpose                                                                                | Read for...                                  |
| --------------------- | -------------------------------------------------------------------------------------- | -------------------------------------------- |
| `tools/`              | The pipeline. Phase-numbered folders (`p10_*`, `p20_*`, ...) implement the seven stages. | "How does the pipeline work?"               |
| `sql_logic_extractor/`| The parsing engine that powers `tools/`. SQL → AST → normalized → resolved.            | "How does SQL parsing work under the hood?" |
| `data/`               | Input dictionaries (ZC values), sample queries, schema SQL, demo translations.         | "What does the pipeline run against?"       |
| `wiki/`               | Human-curated domain knowledge (Karpathy-style). Distinct from auto-generated `graphify-out/wiki/`. | "What does X mean in healthcare BI?"        |
| `graphify-out/`       | Auto-generated graph of this codebase. Regenerated by graphify hook on each commit. Do not edit by hand. | "Where is X defined?" / "What depends on Y?" |
| `my_notes/`           | User's local sample corpora and scratch notes. Not source of truth.                    | Quick local tests.                           |
| `notebooks/`          | Jupyter notebooks for interactive runs and demos (typically Fabric notebooks).        | "How do I run this in a notebook?"           |
| `tests/`              | Top-level integration tests. Each tool also has its own unit tests under `tools/<x>/tests/`. | "Is the pipeline healthy end-to-end?"        |
| `docs/`               | Running notes, work shipments.                                                         | "Why was X decided?"                         |
| `planning/`           | Daily/weekly/monthly planning notes.                                                   | (rarely read by code authors)                |
| `case_studies/`       | Customer / project case studies (SSIS-to-Fabric migration, etc.).                      | "What real situations has this addressed?"  |

---

## The pipeline (tools/)

The pipeline has **five phases**. Phase folders are named `pN0_<purpose>`
so the file-tree ordering reveals the pipeline order at a glance.

```
p10_extract     SQL files       →  corpus.jsonl (per-view parsed structure)
p20_index       corpus.jsonl    →  unified networkx graph + lexical index
p30_analyze     graph           →  community detection, primary-community
                                    assignments, similarity findings
p40_synthesize  analysis        →  steward-ready markdown artifacts
p50_present     synthesized     →  interactive HTML, visualizations, deck slides
```

A `shared/` folder holds cross-phase utilities (parsing helpers,
schemas, file I/O). An `operate/` folder holds operations work
(parser dev, performance audits, inventory generation, pipeline
validation) -- not part of the pipeline but used by BI devs and
admins running the system.

Ratification of definitions is **not** part of this codebase --
stewards do that in Collibra (or whichever catalog the org runs).
Our pipeline ends at producing evidence packs for the steward
conversation; Collibra owns the decision record.

For details on each phase, see `tools/PHASES.md`.

---

## What this delivers (deliverables by audience)

The five-phase pipeline organizes into **three conceptual layers**,
plus an `operate/` sidecar. Each layer produces specific artifacts
for specific audiences:

### Layer 1 -- CATALOG: "what does this view do?"
**Audience:** BI developers, analysts, anyone trying to understand a
specific existing SQL view.

**Artifacts:**
- `corpus.jsonl` -- structured per-view parse (one JSON object per view)
  from `p10_extract`. Source of truth for everything downstream.
- Lexical term records (`terms.json` + `terms.csv`) from
  `p20_index/term_extraction`. Per-column lexical anchors used by
  search and by p30 analysis.
- Per-view cohort definitions (`cohorts.json` + `cohorts.md`) from
  `p40_synthesize/cohort_extract`. *"What population does this view
  define? In plain English."*
- Per-view dataset chains (`datasets.json` + `datasets.md`) from
  `p40_synthesize/dataset_extract`. *"Scope-by-scope dataflow with
  English column descriptions."*
- The unified networkx graph (in-memory) from `p20_index/graph_builder`.
  The cross-view index built ON TOP of corpus.jsonl.

The legacy 4-tool product line (Tools 1-4 -- column lineage, technical
logic, business logic, report description) is also part of CATALOG,
serving the same audience. It runs alongside (not inside) the pipeline
and parses SQL via `sql_logic_extractor/`.

### Layer 2 -- GOVERN: "what's going on across all our views?"
**Audience:** Data stewards, governance team, IT leadership.

**Artifacts:**
- Community findings (`communities.md` + per-community HTMLs) from
  `p30_analyze` -> `p50_present`. *"Which views cluster into subject
  areas? Which are cross-domain?"*
- Variance findings (*planned*) from `p30_analyze` -> `p40_synthesize`.
  *"How many distinct definitions of 'pregnant patient' exist?"*
- Term disagreement findings (*planned*). *"Same name, different
  meaning across views -- which need to be renamed?"*
- Per-community steward packets (*planned*) from `p40_synthesize`.
  *"What do we discuss in this 30-minute steward session?"*
- Recruitment lists (*planned*). *"Which BI developers wrote the
  variants? Which should be in the steward conversation?"*

These outputs are evidence packs. Stewards consume them, then ratify
the canonical definitions IN COLLIBRA -- not in this codebase.

### Layer 3 -- VISUALIZE: "show me the picture"
**Audience:** Everyone, but different visuals for different audiences.

**Artifacts** from `p50_present` (+ `operate/validate_graph_pivot`):
- Full-corpus graph (`graph.html`) -- the governance-leverage overview.
- Per-community drill-down HTMLs -- focused subject-area views.
- (Planned) Side-by-side overlay -- multiple views' subgraphs aligned
  on shared anchors. The steward-meeting artifact.
- (Planned) Static slide-ready images for exec decks.

All HTML is self-contained (CDN-free) so it works offline on locked-down
healthcare laptops.

### Operate sidecar: "is the pipeline healthy?"
**Audience:** BI devs and admins running the system; parser developers.

**Artifacts** in `tools/operate/`:
- Parse-health triage: `preflight_check.py`, `diagnose_parse_failure.py`
- Parser maintenance: `auto_propose_rule.py` (+ hypotheses bank)
- Performance audits: `timing_audit.py`
- Inventory + manifests: `inventory_manifest.py` (used tables / ZC / columns)
- Pipeline validation: `validate_graph_pivot.py`, `check_zc_lookups.py`

These do not participate in the pipeline. They run on-demand to keep
the system healthy.

---

## Reading order for a new contributor

1. **This file** (`ARCHITECTURE.md`) -- top-level orientation.
2. **`tools/PHASES.md`** -- phase-by-phase guide.
3. **`wiki/index.md`** -- domain knowledge if working on healthcare-specific code.
4. **`tools/p10_extract/__init__.py`** -- entry point of the pipeline.
5. Follow imports forward through each phase.
6. For one-off diagnostics: **`tools/diagnostics/validate_graph_pivot.py`**
   is the most heavily-commented file and a good place to learn the codebase
   patterns.

---

## Conventions

### Folder naming

- **Pipeline phases** are prefixed `pN0_` (gaps of 10, leaving room for
  inserted phases without renumbering). Phase folders sort in pipeline
  order in any file tree.
- **Cross-cutting utilities** live in `tools/shared/`, not in any
  individual phase folder.
- **Diagnostics** (validation, sanity checks, triage) live in
  `tools/diagnostics/`. They are NOT part of the pipeline.
- **Tests** live in a `tests/` subfolder of each phase
  (`tools/p10_extract/tests/`, etc.), plus top-level integration tests in `tests/`.

### Imports

Imports use the phase-prefixed path:

```python
from tools.p10_extract.parser import parse_view
from tools.p20_index.graph_builder import build_corpus_graph
from tools.p30_analyze.communities import detect_communities
```

The phase number is visible in every import line, so when scanning a
script you can see which phases it touches.

### Code style

- **Function-level docstrings + inline comments are required** in this
  project. This overrides the typical "well-named identifiers do the
  explaining" rule -- the codebase is meant to be readable by a Python
  learner.
- Prefer plain idioms over clever one-liners. Comprehensions only when
  they read more clearly than the equivalent loop.
- Explain Python-specific quirks (mutable defaults, `__init__.py`,
  decorators, etc.) when they appear.

### Output paths

- The pipeline writes to a configurable `output_dir`. Defaults in
  CLI tools point under `/tmp/` or the cwd for local runs.
- In Fabric, the typical convention is
  `/lakehouse/default/Files/<pipeline_run_name>/...`.

---

## Where the auto-generated artifacts come from

- **`graphify-out/`** is regenerated automatically by a post-commit hook
  (the `graphify` skill). It contains a code-AST graph of the repo,
  community structure, and a generated wiki. Do not edit by hand.
- **`graphify-out/wiki/`** is auto-generated documentation of the
  codebase itself. Distinct from `wiki/` (the human-curated domain wiki).

---

## What this repo is NOT

- **Not a runtime service.** Tools run on-demand, typically in Fabric
  notebooks against a lakehouse corpus.
- **Not an ML model.** The analysis is graph-theoretic and rule-based,
  not learned. (LLM integration is a future possibility but
  intentionally out of scope for the pilot.)
- **Not a Collibra connector.** Outputs are meant to be ratified
  manually by stewards before any catalog publication.
- **Not a SQL execution engine.** The pipeline parses SQL with sqlglot;
  it does not run queries.

---

## Status (2026-05-17)

The repo is **mid-restructure**. The pipeline phase folders
(`tools/pN0_*`) exist as skeletons after Phase 0 of the restructure.

The pre-restructure architecture had tools spread across ~20 sibling
folders in `tools/` (`extract_corpus/`, `similarity/`, `cohort_extract/`,
etc.). Those folders still exist and continue to work; they will be
migrated into the new phase structure incrementally over the next
several restructure phases (see `tools/PHASES.md` migration table for
the current state of each legacy tool).

A **graph-pivot validation experiment**
(`tools/diagnostics/validate_graph_pivot.py`) ran in May 2026 against
the user's real 130-view corpus and returned PASS. The restructure
implements the architecture that experiment validated.
