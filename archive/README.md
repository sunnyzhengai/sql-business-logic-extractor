# Archived Scripts

These scripts have been superseded by newer implementations:

| Archived Script | Superseded By | Reason |
|----------------|---------------|--------|
| `compare.py` (L3) | `compare_lineage.py` | L5-based comparison is more accurate - CTEs are fully inlined to base tables |
| `translate.py` (L4) | `llm_translate.py` | LLM-based translation with clarity_schema.yaml is more accurate and flexible |
| `metadata.py` | `clarity_schema.yaml` | Real Epic Clarity data dictionary replaces mock metadata |

These files are kept for reference but should not be used in production.
