Previous row's Hospital Discharge Time, for same Patient Identifier, ordered by Hospital Admission Time
# sql: LAG(HOSP_DISCH_TIME) OVER (PARTITION BY PAT_ID ORDER BY HOSP_ADMSN_TIME)
# category: window
# subcategory: lag
# base_columns: ['HOSP_ADMSN_TIME', 'HOSP_DISCH_TIME', 'PAT_ID']
# base_tables: []
# unknown_nodes: []
# unknown_columns: ['HOSP_ADMSN_TIME', 'HOSP_DISCH_TIME', 'PAT_ID']
