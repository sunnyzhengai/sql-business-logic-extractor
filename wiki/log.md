# Wiki log

Append-only ledger. Newest at top. One line per event.

Format: `YYYY-MM-DD  <kind>  <summary>  [affected: page1, page2]`

Kinds: `ingest` · `decide` · `commit` · `lint`

---

2026-06-07  commit  6a34fdb notebooks: build_clarity_schema -- join CLARITY_TBL+CLARITY_COL into a LOCAL schema  [affected: notebooks/build_clarity_schema.py NEW, .gitignore (+data/dictionaries/CLARITY_*.csv, clarity_schema.json, clarity_schema.yaml). Sunny has the real Epic Clarity dictionary as two CSVs in Fabric (/lakehouse/.../data/dictionaries/CLARITY_TBL.csv + CLARITY_COL.csv) and wants to replace the mockup schema. KEY GOVERNANCE CALL: the GitHub repo is PUBLIC and the Clarity dictionary is Epic's proprietary content -- it must NOT be committed. Decided: keep the dictionary + generated schema LOCAL in Fabric, point describe_one_view.py SCHEMA_PATH at it; only the converter code lives in git; added .gitignore guards so the CSVs/generated schema can't be committed by accident. JOIN: CLARITY_COL.TABLE_ID = CLARITY_TBL.TABLE_ID (1 table : N columns). Sunny's headers revealed (a) the Fabric CSVs have NO header row (bcp/sqlcmd/Spark export omits it) -> read with header=None + explicit column order; (b) her CLARITY_TBL export is the extract-metadata flavor with NO table-description column -> only TABLE_ID->TABLE_NAME from it, table descriptions blank; column descriptions carry the value. Field mapping into the shape patterns/columns.py reads: DESCRIPTION->description, COL_DESCRIPTOR_OVR else COL_DESCRIPTOR->short_description (translator prefers short), FORMAT_INI/FORMAT_ITEM->ini/item, DATA_TYPE(+CLARITY_PRECISION/SCALE)->type. Output JSON ensure_ascii=True (cp1252 re-upload safety). Converter prints parsed shape + sample rows so a column-order or unquoted-comma mismatch in the headerless export is caught immediately. PENDING: Sunny to run it and report the shape/sample print; if CLARITY_COL is a bcp export without quoting, commas inside DESCRIPTION may break tokenizing -> revisit sep/quoting.]
2026-06-07  commit  1e94889 notebooks: describe_one_view -- Tool 4 on any view, engineered vs LLM  [affected: notebooks/describe_one_view.py NEW. Sunny ran fabric_llm_setup Cell 2 and got {'status':'ok'} -- LLM chain (code+SDK+key) confirmed. This is the "test on a real view" cell: unlike demo_llm_vs_engineered.py (pinned to the mockup clarity_schema.yaml), it points at ANY view path with SCHEMA_PATH optional (None = describe from SQL logic only, since Sunny's real Clarity data dictionary isn't uploaded yet). Reuses the env vars set by the setup cell -- generate_report_description(use_llm=True) builds the client from env via make_llm_client(), so no key re-paste. Prints engineered vs LLM business_description + primary_purpose + key_metrics. Footer note: for a reporting proc, run select_into_to_cte(sql) first, then describe.]
2026-06-07  commit  3fd7522 notebooks: fabric_llm_setup -- one-time setup + smoke test for LLM mode  [affected: notebooks/fabric_llm_setup.py NEW. Two-cell Fabric setup so Sunny can turn on LLM-mode view/proc descriptions now that her work permits Azure/OpenAI: Cell 1 %pip installs sqlglot/openai/pyyaml/python-dotenv (alone -- %pip can restart the session); Cell 2 edits 3 constants (REPO_DIR, PROVIDER, API_KEY), adds repo to sys.path (avoids editable-install quirks on the Lakehouse mount), sets provider env vars, imports the package, and does ONE tiny complete_json smoke call to prove code+SDK+key before a real run. Cell 3 (optional) runs demo_llm_vs_engineered.py. WORKFLOW NOTE: Sunny runs Claude on her PERSONAL machine; her WORK machine (where Fabric lives) does not allow Claude and there is no copy-paste path between them -- GitHub is the ONLY bridge, so any runnable cell >a few lines MUST be committed+pushed, not pasted in chat.]
2026-06-07  commit  9d9ce6e notebooks: normalize_one_proc -- run select_into_to_cte against one proc  [affected: notebooks/normalize_one_proc.py NEW. Companion to diagnose_failing_proc.py: reads one specified proc (BOM-aware UTF-8/16/latin-1), runs select_into_to_cte, and prints either VIEW-SHAPED + the rewritten CREATE VIEW ... WITH ... SELECT, or NOT view-shaped + the ProcNotViewShaped reason code so Sunny can paste the verdict back. Edit PROC_FOLDER + FILE_NAME at top. Pushed to main via an isolated git worktree because a parallel agent had switched the main working dir onto feature/llm-provider-neutral-azure-openai mid-session.]
2026-06-07  commit  ace095d LLM mode: provider-neutral adapter (Azure OpenAI / OpenAI / Gemini)  [affected: sql_logic_extractor/llm_client.py NEW (OpenAIClient covering Azure+direct, GeminiClient, make_llm_client), sql_logic_extractor/business_logic.py (translate_column_llm + summarize_llm now call the vendor-neutral complete_json; old Gemini-only make_llm_client removed and re-exported from llm_client; unused Optional import dropped), tools/report_description_generator/cli.py (+--llm-provider flag, builds client up front), pyproject.toml (+openai>=1.0 in ai/llm extras, NEW ai-openai / ai-gemini per-provider extras), tools/PHASES.md (p40 note: LLM mode now available, engineered still the healthcare-safe default), tests (stub-client LLM-path tests in both tool suites + import-guard now asserts openai stays out of sys.modules; tests/test_llm_client.py NEW covering provider resolution/auto-detect + Azure deployment-name handling via a fake openai module), notebooks/demo_llm_vs_engineered.py NEW (side-by-side engineered vs LLM proof-of-value), wiki/concepts/metadata-only-no-phi.md NEW + wiki/index.md link. Context: Tools 3/4 shipped with a DORMANT LLM path wired only to Gemini (gated behind the *_llm features, lazy-imported so a no-LLM build never installs the SDK). Sunny's day-job originally banned LLM APIs (the whole reason summarize_engineered exists); now her work permits Azure OpenAI, so we activated the path. Key design choice (confirmed with Sunny): keep it provider-neutral rather than swap Gemini->OpenAI, because different healthcare customers will have different approved providers -- one complete_json() adapter, provider chosen via SLE_LLM_PROVIDER or auto-detected from which creds are present (AZURE_OPENAI_ENDPOINT -> azure; OPENAI_API_KEY -> openai; GEMINI_API_KEY -> gemini). Healthcare-safe structural guarantee preserved AND extended: engineered mode loads neither google.genai NOR openai. Azure specifics: AzureOpenAI(endpoint, key, api_version default 2024-10-21), model = the DEPLOYMENT name (AZURE_OPENAI_DEPLOYMENT), not a public model id -- a common first-timer trap. IT will be slow to grant enterprise Azure OpenAI, so for the value demo Sunny used a PERSONAL direct-OpenAI key (gpt-4o-mini) on metadata only -- live-verified end-to-end on complex_referral_analytics.sql against the clarity_schema.yaml mockup: engineered dumps the raw SQL comment header + stiff "Limited to:" list; LLM returns fluent prose ("...insights into recent referrals...average wait times, appointment outcomes, and patient risk levels..."). 596 tests pass. Still PENDING: real Clarity CSVs (CLARITY_TBL.csv + CLARITY_COL.csv) not yet uploaded to data/dictionaries/ -- once they land, adapt data/schemas/csv_to_schema.py to join the two on TABLE_ID (it currently expects one pre-joined CSV + CLARITY_COL_INIITM for ini/item, which Sunny doesn't have) and replace the mockup schema. Enterprise target remains Azure OpenAI under a BAA; if their tenant is keyless, the adapter needs an azure-identity token-provider addition (scoped, not yet built).]
2026-06-07  commit  04d0219 proc_normalize: select_into_to_cte -- rewrite CTE-shaped procs to view-shaped SELECTs  [affected: sql_logic_extractor/proc_normalize.py NEW (select_into_to_cte + ProcNotViewShaped), tests/test_proc_normalize.py NEW (12 tests). Follow-up to the diagnose_failing_proc thread: Sunny's failing proc (Behavioral_Intake_PBI.sql) opens with BEGIN -- so it's NOT a strip_procedure_wrapper lookahead miss. Its body is a stage-and-read: IF OBJECT_ID(...) DROP TABLE #x guards, SELECT ... INTO #stage, then a final SELECT from the temp. By survey_proc_categories taxonomy that's a `reporting` proc. Sunny's key insight: in her shop temp tables are used exactly like CTEs (defined once via SELECT INTO, never INSERT/UPDATE'd), so the difference is pure syntax. This normalizer makes that explicit: each SELECT ... INTO #tmp becomes a CTE named tmp, #tmp references rewrite to the CTE name, CTEs prepend to the single terminal SELECT in source/dependency order, output wrapped as CREATE VIEW [schema].[name]. Guards are stripped by regex BEFORE parsing because the IF OBJECT_ID form makes sqlglot abort the entire multi-statement parse ("Unsupported If block syntax"). Safety net: rewrites ONLY when the CTE-equivalence constraint holds; otherwise raises ProcNotViewShaped with a stable reason code (unsupported_statement = INSERT/UPDATE/MERGE/DECLARE mutation; select_into_persistent = SELECT INTO a real table = ETL; temp_redefined; multiple_terminal_selects; no_terminal_select; undefined_temp_reference) so the caller can BUCKET the proc rather than emit a wrong view. So it's a classifier as well as a rewriter. Verified end-to-end through _build_view: lineage resolves main -> cte:stage -> base table (encounter), no phantom #temp node, primary_purpose=Aggregated reporting. NOTE: wrapper/BEGIN-END stripping here overlaps with the existing parsing_rules strip_procedure_wrapper + strip_proc_begin_end_wrapper (commit 1be70fb) -- standalone regex kept so the function is self-contained for notebook use; a future extract_corpus integration should delegate wrapper-stripping to apply_all() and only run the SELECT-INTO->CTE transform. NOT yet wired into extract_corpus (deferred pending a survey_proc_directory run to size the reporting bucket). 12 tests pass standalone. @table-variable staging not handled (only #temp).]
2026-05-27  commit  e5fd132 notebooks: diagnose_failing_proc -- show CREATE PROCEDURE through first 15 body lines  [affected: notebooks/diagnose_failing_proc.py NEW. Sunny's extract_corpus run on her stored-proc folder threw hundreds of "ValueError: Failed to parse SQL: Invalid expression / Unexpected token" lines. The head of one failing file (`Behavioral_Intake_PBI.sql`) showed only a `/* Author: ... Create Date: 10/29/2025 ... */` comment block in the first 15 lines -- the CREATE PROCEDURE keyword and body weren't yet visible, so we can't tell whether strip_procedure_wrapper's lookahead is missing a body keyword (likely SET NOCOUNT ON or IF EXISTS that I didn't anchor) OR whether the body has T-SQL flow control sqlglot can't parse no matter how cleanly we strip. This notebook reads one specified proc, hops past everything before CREATE [OR ALTER] (PROCEDURE|PROC|FUNCTION|TRIGGER), and prints (a) the declaration block through AS so we see the param shape and any WITH options, and (b) the first 15 non-blank lines of the body so we see what keyword it actually opens with. Tolerant UTF-8/UTF-16/latin-1 reader for SSMS's default encoding. Sunny edits PROC_FOLDER + FILE_NAME at the top, pastes output back -- usually a one-line regex tweak from there.]
2026-06-03  commit  80a77d7 corpus_search: unified entity + text search (Phase 1+2)  [affected: tools/p50_present/corpus_search.py NEW (build_search_index + write_corpus_search), tools/p50_present/tests/test_corpus_search.py NEW (5 tests), tools/operate/validate_graph_pivot.py (+import, +Phase 8 writing corpus_search.html at output root, +corpus_search in return dict, +docstring artifact list), tools/p50_present/corpus_map.py (+"Search the corpus >>" header link), tools/operate/tests/test_validate_graph_pivot.py (end-to-end asserts corpus_search.html exists), docs/mocks/corpus_search_demo.html NEW (live demo from the real code against a 3-view fixture), docs/mocks/README.md updated. Single search box, results grouped by where the match was found: table name, view name, view description, column name, filter expression (raw + English), inline comment, and ZC-lookup name (so typing "Active" hits views whose filter `STATUS_C = 1` was resolved to "Active" by zc_values.csv). Embedded JSON index + vanilla-JS live filter; no backend, no build step, self-contained HTML works on download. Sunny approved unified search (Option A) from the corpus_search_mockup design discussion. Phase 3 (CLARITY_EDG diagnosis-code resolution) and Phase 4 (Caboodle/Registry naming-pattern badges) deferred to follow-up commits. 574 tests pass (5 new corpus_search + 1 end-to-end assertion).]
2026-06-02  commit  1be70fb parsing_rules: strip CREATE PROCEDURE + BEGIN/END wrappers so view-shaped procs flow through extract_corpus  [affected: sql_logic_extractor/parsing_rules/rules.py (+2 rules), parsing_rules/fixtures/strip_procedure_wrapper/, parsing_rules/fixtures/strip_proc_begin_end_wrapper/. Sunny confirmed her shop's stored procs are all view-shaped (proc vs view is a reporting-tool choice, not a structural one). So procs flow through the existing extract_corpus pipeline if we strip the two pieces of proc-specific boilerplate. (1) strip_procedure_wrapper: removes `CREATE [OR ALTER] PROCEDURE name [(params)] [param_list] AS`. AS is identified by lookahead at a SQL body keyword (BEGIN|SELECT|WITH|DECLARE|RETURN|INSERT|UPDATE|DELETE|MERGE|EXEC) so embedded `AS TableType` constructs in table-valued parameters don't trip us. (2) strip_proc_begin_end_wrapper: outermost BEGIN/END (CASE/TRY/IF nested BEGIN/END preserved). End-to-end test on three sample procs (read-only, reporting with CTE, BEGIN/END wrapped): all three produce clean ViewV1 entries with the expected scopes/joins/columns. Now extract_corpus(input_dir=[views..., procs...]) at any mix of folders yields one combined corpus; procs render identically to views in every downstream artifact. 569 tests pass.]
2026-06-02  commit  5a8b4d5 community_overview: better spreading -- Kamada-Kawai for moderate graphs, bumped k for big ones, percentile clipping  [affected: tools/p50_present/community_overview.py (frequency_layout rewrite), docs/mocks/community_overview_demo.html regenerated against a 5-view 15-table scenario. Yang's feedback: "nodes are crowded in the center, where there's a large space blank" -- classic spring-layout pathology where attractive forces collapse dense subgraphs while a few outliers stretch the bounding box, leaving the central mass cramped. Fix: (1) For graphs <= 150 nodes use networkx.kamada_kawai_layout (energy-min over shortest-path distances, much more uniform spacing than spring). (2) Spring fallback uses k=2.0/sqrt(n) (vs default 1/sqrt(n)) and iterations=300, giving the system more breathing room. (3) Post-layout normalization uses 5th/95th PERCENTILE clipping instead of min/max -- outliers no longer dictate the bounding box, so the central mass spreads to fill the canvas. KK is naturally deterministic; spring keeps the fixed seed. 563 tests pass.]
2026-06-01  commit  45c3de4 community_overview v2: stripe-click spotlights substrate in-page, labels hidden by default on big communities  [affected: tools/p50_present/community_overview.py (substantial rewrite of the HTML template + JS), tests updated, docs/mocks/community_overview_demo.html regenerated. Yang's three pieces of feedback after the first look: (1) stripe links 404'd; clicking should highlight in-page, not navigate; (2) hundreds of tables = unreadable cloud, label overlap; (3) clicking a per-view stripe should make the relevant tables readable. Solved together by inverting the model: substrate is now the PRIMARY interactive canvas; stripes are CLICKABLE FILTERS. New JS: substrate <g data-table=> and <line data-edge=> elements are recolored on stripe-click via `spotlight(viewName)`; non-view nodes/edges fade out, view labels appear. `Show all` button restores the frequency view. Labels default-hidden when n_nodes > label_threshold (default 30) -- cloud-shaped large communities stay readable through CLICK + HOVER, not static labels. Adaptive canvas size: 820x460 (≤30), 1100x660 (31-80), 1400x880 (81-180), 1700x1080 (>180). Stripes are now plain <div class="stripe"> with data-view-stripe attr; the old <a> wrap is removed. Optional "Open detail" fallback link below each stripe still points at the per-view shape HTML for callers who want the full unfolding. 563 tests pass.]
2026-06-01  commit  5667185 community_overview: per-community big-picture HTML (substrate + view stripes)  [affected: tools/p50_present/community_overview.py NEW (5 entry points), tools/p50_present/tests/test_community_overview.py NEW (11 tests), tools/operate/validate_graph_pivot.py (+import write_community_overview / community_color, +Phase 6 emitting community_overviews/community_NN_*_overview.html, +Phase 7 corpus_map now links to overviews instead of straight to shapes, +community_overviews entry in return dict, +overview path in print summary, +docstring artifact list), tools/operate/tests/test_validate_graph_pivot.py (end-to-end asserts overviews/ populated), docs/mocks/community_overview_demo.html NEW. The artifact answers the BI-developer's questions Yang articulated in the brainstorm: 'What's the CORE of this community? Which views are siblings? What does each view add on top?' One HTML per community: large frequency-colored substrate at the top (node size + opacity scaled by view-count -- tables used by all views are big and saturated, tables used by 1 view fade to the periphery as outliers), per-view stripes below (small thumbnails at the SAME layout coords with lit/faded subset, each stripe wrapped in <a> linking to the per-view shape HTML anchor for one-click drill-down). Substrate built via build_community_substrate; layout via networkx spring_layout (seeded=42); colors continuous with corpus_map (community_color(idx) for the community's hue). Navigation hierarchy is now: corpus_map.html -> community_overviews/community_NN_*_overview.html -> community_shapes/community_NN_*_shapes.html#view-X. 560 tests pass (11 new).]
2026-05-31  commit  12d353d extract_corpus: accept list of input directories for full-cohort extraction  [affected: tools/p10_extract/batch.py + tools/p10_extract/tests/test_batch.py. Yang's MyChart pilot of 11 views was a proof-of-concept; now switching to the FULL cohort across data/views_reporting + data/views_cookrpt (196+ foundation views). Modified extract_corpus's input_dir parameter to accept either a single dir string (existing behavior preserved) OR a list/tuple of directory paths. When a list, globs *.sql from each in order, dedupes by file stem (first-folder-wins, prints warning on duplicate), and writes a single combined corpus.jsonl. Missing dirs in the list are warned-and-skipped rather than erroring. Progress file logs all input dirs when multi-folder. 3 new tests: list-of-dirs success, dedup-first-wins, tolerance-of-missing-dirs. 549 total pass. Yang's call: extract_corpus(['/lakehouse/default/Files/data/views_reporting', '/lakehouse/default/Files/data/views_cookrpt'], output_path='...').]
2026-05-31  commit  0ce5d71 notebooks: debug_one_view_expansion -- fall back to foundation folders when host not in corpus  [affected: notebooks/debug_one_view_expansion.py. Yang's first attempt failed with 'host view not found' -- the view he wanted to debug was in data/views_reporting / data/views_cookrpt (foundation views NOT in the corpus.jsonl), not in his 11-view MyChart pilot corpus. Updated Cell 1's lookup to try TWO places in order: (1) corpus.jsonl as before; (2) if not found, scan VIEW_SOURCE_DIRS for `{HOST_VIEW_NAME}.sql` and parse on-demand via parse_view_for_shape. Prints which source the host loaded from. If still not found, the error block lists BOTH the corpus view names AND the first 30 SQL file stems from each foundation folder so the user can spot the right name.]
2026-05-31  commit  0e7d5dd notebooks: debug_one_view_expansion -- pin why a specific view's foreign refs don't inline-expand  [affected: notebooks/debug_one_view_expansion.py NEW. Yang found a view whose CTE references another view but the foreign view isn't getting inline-expanded -- even after probe_external_view_paths.py confirmed the paths and 2 distinct foreign-view matches. Need a focused trace for ONE view. Five cells: (1) load corpus, find HOST_VIEW_NAME, dump its raw scopes -- shows what the extractor captured. (2) load external_view_lookup, show bare-key map sample. (3) for every reads_from_tables / join.right_table across all host scopes, normalize via _bare_view_key and report HIT/miss against the external lookup. (4) build the host shape with external_view_lookup and walk every placeholder node + cross-scope edge -- print all scope_ref placeholders with target_view_name / target_scope_id, then list cross-scope edges. (5) for each unexpanded placeholder, diagnose: empty target_view_name (no detection), target not in lookup (impossible if detection fired), or expansion ran but produced no scopes (inner parse-fail / cycle skip).]
2026-05-31  commit  28b74c5 notebooks: probe_external_view_paths -- find the working absolute path + verify name normalization vs corpus  [affected: notebooks/probe_external_view_paths.py NEW. Goes one step beyond diagnose_external_view_expansion.py. Cell 1 probes a list of candidate absolute paths (Yang's Fabric path SZ_SQL_Logic/default/Files/data/* + typical /lakehouse/default/Files/* variants + workspace-named lakehouse mount + paths without data/ prefix) and prints which contain .sql files. Also enumerates /lakehouse top-level for orientation when none of the candidates hit. Cell 2 calls load_external_views(view_source_dirs=working_paths, verbose=True) so parse failures surface. Cell 3 loads corpus.jsonl, walks every view's reads_from_tables + join.right_table, applies _bare_view_key normalization to both sides, and counts how many references would MATCH an external view after normalization -- catches the case where files are loaded but name mismatch means no inline expansion fires. Cell 4 prints the exact view_source_dirs=[...] line to paste into run_validation.]
2026-05-31  commit  93bea31 view_resolver + run_validation: absolute-path override for view-of-view expansion  [affected: tools/operate/view_resolver.py + tests, tools/operate/validate_graph_pivot.py, notebooks/diagnose_external_view_expansion.py. Yang's Fabric run loaded 0 external views from 0 .sql files -- the cwd-relative VIEW_SOURCE_DIRS lookup doesn't work when Path.cwd() doesn't point at the repo root (common in Fabric notebooks). Added `view_source_dirs` parameter to load_external_views (accepts absolute paths, bypasses the cwd-relative resolver entirely) and threaded it through run_validation so Yang can pass `view_source_dirs=['/lakehouse/default/Files/views_reporting', ...]` directly. Missing absolute paths still silently skipped. Updated diagnostic notebook's verdict block to show both ad-hoc (`load_external_views(view_source_dirs=[...])`) and production (`run_validation(view_source_dirs=[...])`) override patterns, plus a `find` snippet to locate the SQL folders when their path is unknown. 546 tests pass (2 new tests covering absolute-path mode + tolerance of bogus paths).]
2026-05-31  commit  44f503c notebooks: diagnose_external_view_expansion -- pin why view-of-view expansion isn't firing  [affected: notebooks/diagnose_external_view_expansion.py NEW. Yang synced + ran the recursive-expansion commit but no inline expansion appeared in his community_shapes/ output. Notebook walks the five likely causes: (1) validate_graph_pivot.py wasn't synced -- local copy doesn't call load_external_views, (2) data/views_* folders aren't at cwd-relative path the resolver expects (Fabric's Path.cwd() varies), (3) folders exist but no .sql files, (4) files exist but all parse-fail (rare; tolerated silently), (5) name normalization mismatch between corpus view_name and external file stem. Each step prints what it found, then a final verdict block tells the user which scenario they hit and what to do. Uses load_external_views(verbose=True) so silent parse failures get logged. Paste into a Fabric cell after force-reloading modules; runs in <1s on typical corpus sizes.]
2026-05-31  commit  c4c6222 view-of-view recursive inline expansion (depth=1)  [affected: tools/operate/view_resolver.py NEW (load_external_views / parse_view_for_shape), tools/operate/tests/test_view_resolver.py NEW, tools/p50_present/view_shape.py (build_view_shape gains external_view_lookup + max_expansion_depth + _expanded_so_far cycle guard; _inline_expand_externals / _merge_external_shape_into_host helpers; 'external' label kind = 'View: X'), tools/p50_present/tests/test_view_shape.py (4 new expansion tests: inline, label-breadcrumb, depth=0-no-expand, cycle-safe), tools/operate/validate_graph_pivot.py (+import load_external_views, calls it before the community_shapes loop, passes to write_community_shapes). view_resolver parses every .sql under data/views_reporting / data/views_cookrpt into minimal shape-ready ViewV1 dicts via extract+resolve (skipping column English translation and ZC lookups for speed). When the shape renderer finds a view-of-view reference whose target view is in external_view_lookup, it recursively builds that view's shape (depth=1 by default) and MERGES the inner scopes into the host with id-prefixing (`external:V_FOO/main`) and node-id remapping to avoid collisions; a cross-scope edge from the host placeholder connects to the inner main's driver. Cycle guard: the host's own canonical name + all ancestors in the call chain are tracked in `_expanded_so_far`, so V_A->V_B->V_A terminates cleanly. The foreign-view hyperlink stays available regardless of expansion -- modeler can still jump to the foundation view's standalone shape HTML. 544 tests pass.]
2026-05-30  commit  66b9533 corpus_map: clarify footer link target + demo no longer has broken links  [affected: tools/p50_present/corpus_map.py, docs/mocks/corpus_map_demo.html regenerated. Yang reported a 404 when clicking the corpus_map.html footer. Two causes: (1) the docs/mocks/ demo's footer linked to filenames that don't ship alongside it (so htmlpreview viewers 404 on click). Regenerated with community_files=None so the demo shows 'no shapes file' labels instead of dead hyperlinks. (2) Real Fabric output: links work IF community_shapes/ is a sibling of corpus_map.html. Added a small <em>note</em> in the meta line of corpus_map.html telling the reader the links require that sibling folder -- so anyone moving files around knows to keep the whole output dir together. 535 tests pass.]
2026-05-30  commit  5f4e316 corpus_map: Stage 1 corpus landscape -- spring-layout of every table colored by community, footer links to per-community shapes  [affected: tools/p50_present/corpus_map.py NEW (4 public entry points: build_corpus_substrate / force_directed_layout / render_corpus_overview_svg / write_corpus_map), tools/p50_present/tests/test_corpus_map.py NEW (11 tests), tools/operate/validate_graph_pivot.py (+import write_corpus_map, +Phase 6 block writing corpus_map.html at output_dir root, +'corpus_map' in return dict, +docstring artifact list), tools/operate/tests/test_validate_graph_pivot.py (end-to-end asserts corpus_map.html exists), docs/mocks/corpus_map_demo.html NEW (3.5KB demo from the mock 7-table 2-community corpus). The artifact: one HTML per corpus showing every table laid out via networkx spring_layout (seeded=42 for determinism), colored by Louvain community via community_color() from community_html.py (palette reused for consistency). No labels by default -- with 200+ tables labels would overlap to noise -- hover for native tooltip showing table + community. Pale grey edges so community structure surfaces through node clustering and color. Bottom-of-page CSS grid of communities, each with a swatch + hyperlink to community_shapes/community_NN_<top>_shapes.html. The map functions as the modeler's entry point: glance at the whole corpus shape, click a cluster to drill into per-community shapes. 535 tests pass (11 new corpus_map tests + 1 end-to-end assertion).]
2026-05-30  commit  45939ce view_shape: order scopes by reference order, not alphabetic  [affected: tools/p50_present/view_shape.py + tests, docs/mocks/view_shape_demo.html regenerated. Yang's observation on real-corpus output: a view's main referenced CTEs A, B, C in that order but the panel showed them backwards (because the previous layout sorted scopes alphabetically after the root). Fix: new helper _scope_reference_order(shape) walks cross_scope_edges from each root in INSERTION order (which == SQL JOIN/FROM order) and adds each referenced scope the first time it's seen. Wrapper scopes expand to their branch children in lexical order. Unreachable scopes (rare) get appended at the end so they're still visible. layout_shape now uses this instead of `sorted(rest)`. Regression test test_scope_order_follows_reference_order_not_alphabetical: main referencing CTE_Z, CTE_A, CTE_M (in that order) now renders Z/A/M -- not the previous A/M/Z alphabetic sort. 524 tests pass.]
2026-05-30  commit  551a1f8 view_shape: placeholder nodes for consumed scopes + hyperlinks for view-of-view + views_corpus_config  [affected: tools/p50_present/view_shape.py + tests, tools/operate/validate_graph_pivot.py, tools/operate/views_corpus_config.py NEW, docs/mocks/view_shape_demo.html. Three additions answering Yang's two questions on top of v4. (1) ShapeNode gains `kind` ('table' | 'scope_ref') and `target_scope_id` / `target_view_name`. _build_scope's JOIN loop now emits a PLACEHOLDER ShapeNode for every consumed CTE / derived subquery / JOIN-subquery / LATERAL -- main's flow reads top-to-bottom as the SQL author wrote it ('FROM PATIENT JOIN foo'), with a cross-scope dashed edge from the placeholder to the inner scope's driver. Placeholders render as rounded amber rectangles to visually distinguish 'consumed scope' from 'real base table' (blue circles). reads_from_scopes entries not covered by JOIN also emit placeholders. (2) view-of-view detection: build_view_shape accepts a corpus_view_names set; when a SQL reference's bare name matches another view in the corpus, the node becomes a scope_ref placeholder with target_view_name set. render_view_shape_panel accepts view_links dict mapping canonical name -> 'file#anchor'; matching placeholders get wrapped in <a xlink:href> so a click jumps to that view's shape HTML in a new tab. validate_graph_pivot pre-computes the link map across all per-community shape files and threads it through. (3) tools/operate/views_corpus_config.py NEW: VIEW_SOURCE_DIRS = ('data/views_reporting', 'data/views_cookrpt') -- Yang's two SQL source folders for future batch-extract / nested-view-resolution. resolve_view_source_dirs() and find_sql_files() helpers. 523 tests pass (5 new view_shape tests covering placeholder emission, scope_ref visual, view-of-view detection, hyperlink wrapping).]
2026-05-30  commit  5c870d0 extract + resolve + view_shape: CROSS APPLY / LATERAL handled + soft-descend fallback for unknown wrappers  [affected: sql_logic_extractor/extract.py + resolve.py, tools/p50_present/view_shape.py + tests, tests/test_queries.py, docs/mocks/view_shape_demo.html. Bug Yang found in real-corpus output: a view had CROSS APPLY to a subquery; outer table extracted, but the subquery's tables silently vanished. Root cause: extract.py's _decompose_any had an EXPLICIT fall-through for exp.Lateral (which is what CROSS APPLY / OUTER APPLY / LATERAL all parse to) -- it just got logged in unknown_containers and skipped. (1) extract.py: explicit Lateral handler in _decompose_any that descends to lateral.this (the inner Subquery/Select) under context='lateral'. (2) Generic SOFT-DESCEND fallback in _decompose_any for any other unknown wrapper (Pivot, Unpivot, Values, future types): hunts for nested Select/Subquery and recurses. Wrapper-type-specific semantics are sacrificed, but tables inside the wrapper never silently vanish -- which is the safety net Yang asked for. (3) _extract_joins: when right side is exp.Lateral, set right_name=alias and dispatch through _decompose_any with context='lateral' (same shape of fix as the earlier JOIN-subquery patch). (4) resolve.py: lateral aliases tracked in derived_alias_to_id (`lateral:<alias>` id), parallel to from/join. Scope-id synthesis at the recursion site uses the same alias-based id. (5) view_shape v4: _scope_label renders kind='lateral' as 'CROSS APPLY: <alias>' -- T-SQL-familiar naming. Regression test in tests/test_queries.py asserts the lateral subquery's tables are captured AND 'Lateral' no longer appears in unknown_containers. 518 tests pass.]
2026-05-30  commit  a9c47f8 resolve + view_shape: nested set-op branches + vertical tree-list layout  [affected: sql_logic_extractor/resolve.py, tools/p50_present/view_shape.py + tests, docs/mocks/view_shape_demo.html. Two changes shipped together. (1) resolve.py: _emit_scope_recursive now also iterates logic.set_operations and emits each branch as a sub-scope with id `{parent_id}/union:{i}`. Parent's flat reads_from_tables/joins stay populated for backward compat (matrix, community detection). Top-level set-op handling at line 537+ unchanged. (2) view_shape v4: build_view_shape detects wrapper scopes (any scope with child id `{this_id}/...`) and skips rendering them -- the branch sub-scopes handle the visual instead. Cross-scope edge resolution fans out: when a consumer targets a wrapper, emit one edge per branch's driver_node (so both UNION branches connect back to main rather than just the first). Layout flipped from horizontal BFS to vertical tree-list: each scope's nodes get one row each via pre-order DFS, depth = horizontal indent, labels render to the RIGHT of each circle. Right-angle edge paths mimic file-tree connectors. Solves the long-label overlap issue + the UNION-inside-CTE second-branch orphan bug in one change. New tests: test_union_in_cte_renders_branches_skipping_wrapper, test_wrapper_scope_label_has_path_breadcrumb. 516 tests pass.]
2026-05-30  commit  bf1871e view_shape v4 + bug fixes: query-unfolding model, JOIN-subquery descent  [affected: sql_logic_extractor/extract.py + resolve.py, tools/p50_present/view_shape.py + tests, tests/test_queries.py, docs/mocks/view_shape_demo.html, plus tag view_shape_v3 on commit 380ce57. Three changes shipped together because they're entangled. (1) extract.py:_extract_joins now descends JOIN-clause subqueries via _decompose_any (the only subquery entry point that wasn't recursing -- FROM, WHERE/EXISTS/IN, SELECT-scalar all did) AND uses the subquery's alias as right_table instead of embedding the multi-line SQL body. Regression test: test_23b_join_clause_subquery_descended. (2) resolve.py extends derived_alias_to_id to track JOIN-subquery aliases (`join:<alias>` ID, parallel to FROM-subquery's `derived:<alias>`); the scope-id synthesis at line 697+ stays in lockstep so right_alias resolves cleanly. (3) view_shape.py rewritten for the unfolding model -- replaces v3's deduped substrate (each table = ONE node, lit/faded against shared layout). Now each SQL occurrence is its OWN ShapeNode (self-joins produce two distinct nodes), CTEs/subqueries are nested ShapeScopes with their own bounding-box clusters, edges respect SQL join order, cross-scope edges connect a consumer node to the referenced scope's driver. Overlay tri-color mode dropped (no shared substrate to overlay onto); pair / show-all preserved. Layout: scope clusters stack vertically; each scope is a left-to-right BFS tree from its FROM driver. 514 tests pass (10 new view_shape tests, one test_27 assertion updated for the new corpus shape).]
2026-05-30  commit  967bfb1 onprem_extract: auto-detect ODBC driver (18 / 17 / Native Client 11)  [affected: scripts/onprem_extract/extract_tables.py + extract_ddl.py. Yang hit IM002 "data source name not found" on work laptop because the scripts hardcoded "ODBC Driver 18 for SQL Server" but the laptop only has Driver 17 + Native Client 11.0. Fix: added pick_driver() helper to both scripts that walks pyodbc.drivers() in a preference list (18 → 17 → Native Client 11 → legacy "SQL Server") and returns the first match. Encrypt/TrustServerCertificate options are only appended for the modern "ODBC Driver" family since the legacy "SQL Server" driver rejects them. Raises a clear error listing installed drivers if none match. No wiki concept page touched -- pure script ergonomics fix.]
2026-05-30  commit  380ce57 view_shape: Left/Right dropdowns now control on-screen panel positions  [affected: tools/p50_present/view_shape.py + demo regenerated. Bug Yang found: in pair mode, picking VW_C in the Left dropdown and VW_B in the Right dropdown still rendered VW_B leftmost on screen (alphabetic DOM order). Root cause: showPair() in the embedded JS only toggled visibility -- panels stayed in their original DOM order, which is sorted by view_name. Fix: set CSS flexbox `order=1` on the Left-selected panel and `order=2` on the Right-selected panel so the visual position matches the dropdown label. showAll() resets order back to '' (default) so the scroll-through view returns to alphabetic order. 521 tests pass.]
2026-05-30  commit  d12811d view_shape: filter SQL-fragment leaks out of the shape graph  [affected: tools/p50_present/view_shape.py + tests. Bug Yang found: nodes in the shape graph labelled "HSP_ACCOUNT_ID IS NULL WHERE 1 = 1" and similar SQL-clause leaks. Root cause: extract.py occasionally captures filter predicates / `1 = 1` placeholders / CTE-definition fragments as 'tables' in reads_from_tables. community_matrix.py already has _is_real_table_name as a renderer-side guard, but view_shape.py wasn't using it. Fix: import _is_real_table_name into view_shape, apply it inside _bare_table so any non-table string coerces to "" -- and since every caller (driver resolution, node-add, anchor list, column-ref resolution) already guards with `if bare:`, the filter cascades through automatically with no other code changes. Test added with mixed real/garbage reads_from_tables verifies only real tables survive. 521 tests pass.]
2026-05-30  commit  c77aa1c view_shape: fix UNION-CTE branches orphaning the second branch's tables  [affected: tools/p50_present/view_shape.py + tests. Bug Yang found in real-corpus output: in a CTE-wrapped UNION (`WITH combined AS (SELECT FROM A UNION SELECT FROM B) ... JOIN PATIENT P ON C.PAT_ID = P.PAT_ID`), the first branch's table connected to PATIENT in the shape graph but the second branch's table appeared as an orphan node. Root cause: post-fc904a6, the CTE scope has `reads_from_tables=[A, B]` and no internal joins, but view_extended_tree only emitted ONE cross-scope edge from main to the CTE's *first* base table. Fix: new helper `_inner_scope_anchors(inner)` -- if the inner scope has its own joins, return just the driver (others reachable via internal edges); if no joins but multiple bases (UNION case), return ALL bases. Applied symmetrically in the joins loop and the reads_from_scopes loop so both code paths fan out. Plus a defensive fix: don't `continue` when the outer scope's driver is empty -- `_join_left_table` can still derive the LEFT side from the join's own column refs. Two regression tests added; 520 total pass.]
2026-05-29  commit  99609a9 view_shape v3: overlay mode (tri-color 2-view diff in one SVG)  [affected: tools/p50_present/view_shape.py + tests + docs/mocks/view_shape_demo.html. Third compare mode added on top of v2's pair / show-all. Approach: render the overlay SVG SKELETON once per community with stable data-node="<table>" / data-edge="A||B" attributes on every element, embed per-view {nodes, edges} sets as JSON, then a small recolor JS rewrites circle/path fills based on the selected pair. Tri-color encoding: #2c7fb8 blue = in both views, #16a085 teal = Left only, #e67e22 orange = Right only, #dcdcdc grey (dashed) = substrate-only (in neither chosen view). Avoids C(N,2) pre-rendering -- one skeleton scales to any pair without re-serialization. Color legend strip shown only when overlay mode is active. Dropdowns now trigger overlay re-render OR pair-toggle depending on active mode. v2 (commit fe1fe1a) preserved; v1 (commit 1877425) also still tagged view_shape_v1. 518 tests pass.]
2026-05-29  commit  fe1fe1a view_shape v2: 2-pane compare picker, smaller panels, drop substrate  [affected: tools/p50_present/view_shape.py + tests + docs/mocks/view_shape_demo.html. v1 (commit 1877425, tagged view_shape_v1) preserved for revert. Three changes from Yang's second look: (1) Panels too big -- shrunk _COL_SPACING 150->105, _ROW_SPACING 80->64, _NODE_RADIUS 16->13 so two typical panels fit comfortably side-by-side on a ~1100px browser. (2) Removed the redundant substrate-only panel from the top -- every per-view panel already renders the substrate as the faded grey background, so the separate "everything lit" reference was visual noise. (3) New compare picker: Left/Right view dropdowns at the top, Show-selected-pair and Show-all buttons, vanilla ~20-line JS that toggles each panel's .hidden class based on dropdown selection. Default state: first two views selected (alphabetic) so the page opens "ready to compare". Each panel wrapped in `<div data-view="VIEW_NAME">` for JS targeting. Next step on the table: 2-view OVERLAY mode (tri-color: overlap / A-only / B-only) for tighter shape diffs.]
2026-05-29  commit  1877425 view_shape: readability fixes from Fabric user-test  [affected: tools/p50_present/view_shape.py + docs/mocks/view_shape_demo.html regenerated. Three issues from Yang's first look at real-corpus shapes: (1) white labels inside blue circles got lost / truncated against the circle boundary -- moved labels BELOW the circle in dark text on the panel's white background, no clipping, with native browser tooltips via <title> for hover. (2) Many-view communities (7+ views in one row) shrunk panels to unreadable sizes -- removed CSS `width:100%` stretch, added explicit width/height attrs on SVG so each panel renders at its natural pixel size, switched .grid from grid-template-columns to flex-wrap. (3) Single-view panels filled the screen -- same fix; the SVG renders at its natural size, doesn't stretch. Bonus: added a TOC of view names at the top with anchor links into each panel so a reader can jump straight to one. Smaller circles (16 vs 22 radius), wider column spacing (150 vs 130), taller row spacing (80 vs 60) to accommodate the below-circle label band. 516 tests pass.]
2026-05-29  commit  3d1d118 operate/validate_graph_pivot: wire view_shape as Phase 5 of run_validation  [affected: tools/operate/validate_graph_pivot.py (+import write_community_shapes, +Phase 5 block after community_matrices that writes one community_NN_<top_table>_shapes.html per community, +"community_shapes" entry in return dict, +shapes_dir line in the print summary, +docstring update listing the new artifact); tools/operate/tests/test_validate_graph_pivot.py (end-to-end test now asserts community_shapes/ is populated). Same loop shape as the matrix block: iterates `analyses`, filters to `primary_views`, names files via `_safe_filename(top_table)`. Builds from the FULL ViewV1 dicts (not the matrix-renderer's view_data) since shape extraction needs scope tree + base_columns lineage. After Yang re-runs run_validation in Fabric, the per-community side-by-side join graphs land in `<output_dir>/community_shapes/`.]
2026-05-29  commit  837bb43 tools/p50_present: view_shape -- per-view "extended tree" + community substrate side-by-side  [affected: tools/p50_present/view_shape.py NEW (5 public entry points: view_extended_tree / community_substrate / hierarchical_layout / render_view_shape_panel / write_community_shapes); tools/p50_present/tests/test_view_shape.py NEW (13 tests against the 4 mock views from the design conversation); docs/mocks/view_shape_demo.html NEW (rendered demo of A/B/C/D mock views, openable in any browser). The artifact: per community, compute the UNION of base tables + join edges across primary views, lay it out ONCE hierarchically (left-to-right BFS from most-connected table, deterministic across runs), then render N panels (one per view) re-using the same coords -- lit nodes/edges for what THIS view uses, faded for the rest. CTEs / derived subqueries are flattened via base_columns lineage so two views that wrap the same shape differently produce identical extended trees (validated: VW_A == VW_B). Join chains pick the LEFT side via JoinV1.columns rather than always using the scope driver (caught the chain-rooting bug on VW_D's PATIENT->ZC_PATIENT_STATUS). EXISTS / IN subqueries excluded (filter dependencies, not data flow). Designed for Fabric modelers reading variance/coverage across same-community views; modeling decisions stay with the human.]
2026-05-28  commit  e1e6912 notebooks: diagnose_scope_table_collision -- test the table/CTE name-clash hypothesis  [affected: notebooks/diagnose_scope_table_collision.py NEW. Diagnostic for the "two tables missing from matrix table axis even though resolve has them" symptom. Hypothesis: graph_builder.py:185 demotes a READS_FROM_TABLE edge to REFERENCES_SCOPE when the bare table name collides with a CTE / derived-scope bare name in the same view. Notebook reads one view from corpus.jsonl, mirrors _collect_scope_names exactly, prints the collision set + every reads_from_tables entry, and emits a per-table verdict: upstream bug vs. graph_builder demotion vs. downstream-filter bug. Built so Yang can confirm the hypothesis in Fabric without spinning up trace_one_view.py again.]
2026-05-28  commit  6b3c399 corpus: surface filter / join / GROUP BY / ORDER BY column refs  [affected: sql_logic_extractor/corpus_schema.py + extract.py + resolve.py, tools/p10_extract/batch.py, tools/p50_present/community_matrix.py + tests. FilterV1 and JoinV1 gain `columns: tuple[ColumnRefV1, ...]` (column / table / table_alias). Resolver builds an alias-to-real-table map per scope and attaches alias-resolved refs to WHERE / HAVING / JOIN ON predicates; GROUP BY and ORDER BY clauses now emit synthetic Filter entries with kind=group_by/order_by so their columns ride the same pipe. build_view_data merges these with SELECT-output base_columns, deduped by (table, alias, column), rendered as TABLE(alias).COLUMN -- self-join on PAT_ENC A vs PAT_ENC B is no longer collapsed into one row. Backward-compat: corpora without the columns field default to (). 503 tests pass.]
2026-05-25  commit  545f357 Add on-prem extract helper scripts  [affected: scripts/onprem_extract/ NEW (README + extract_tables.py + extract_ddl.py). Two laptop-side scripts so Yang can stop hand-uploading view/proc DDL into Fabric Lakehouse. extract_tables.py dumps a configured list of (schema,table) tuples to CSV via pyodbc. extract_ddl.py queries sys.sql_modules + sys.objects + sys.schemas in one shot and writes one .sql per object under ddl_dump/<schema>/{views,procs}/. Both default to Trusted_Connection=yes and fall back to SQL_USER / SQL_PASSWORD env vars. Path chosen because Fabric notebooks can't reach on-prem (no line-of-sight); on-prem data gateway via pipelines is the "real" answer but requires IT setup, this unblocks immediately.]
2026-05-24  commit  c3e43e1 docs: add parsing_field_guide -- 7-pattern reference for SQL parse failures  [affected: docs/parsing_field_guide.md NEW. Commercial asset for customer-facing onboarding. Captures this session's operational knowledge as portable markdown organized one card per trap (Symptom / Detection / Root cause / What the pipeline does / Prevention). Includes triage flowchart, add-a-pattern template, cross-refs to canonical code (rules.py, preprocess_ssms, diagnostic notebooks). Doc is narrative; code stays canonical. Audience is a healthcare BI engineer at a customer shop, not a sqlglot expert. Cards 1-7: UTF-16 BOM, SSMS preamble, multi-line CREATE/AS, column-list divider, T-SQL alias=expr, Fabric mount read-only, module cache stickiness.]
2026-05-24  commit  8cf29e5 parsing_rules: allow comments / separators between column list and AS  [affected: sql_logic_extractor/parsing_rules/rules.py. MyChart views have a `-- ---------` divider line between the closing `)` of the explicit column list and the AS keyword. The previous create_view_explicit_column_list pattern required `\)\s*\bAS\b` -- only whitespace between -- so the rule silently failed and the whole column list + divider + AS leaked into the cleaned SQL. One-char regex change: `\s*` -> `[\s\S]*?` (non-greedy any-char). Now consumes any content between `)` and `AS`. All 11 parsing_rules tests pass.]
2026-05-24  commit  3263ffc notebooks: add inspect_one_view_after_preprocess -- isolate cache vs shape  [affected: notebooks/inspect_one_view_after_preprocess.py NEW. Force-evicts sql_logic_extractor + tools.* from sys.modules so stale imports can't serve old code. Verifies the fix's sentinel string is loaded, runs preprocess_ssms on a user-supplied view file, prints 30 lines of cleaned output with repr() so leading whitespace / bare AS / stray tokens are visible. Built for Yang's MyChart pilot debug session.]
2026-05-24  commit  a4e38bc preprocess_ssms: handle blank lines between CREATE and bare AS  [affected: sql_logic_extractor/resolve.py. Previous fix (69c4b09) checked `not clean_lines` which was too strict -- SSMS puts blank lines between CREATE and the bare AS, so by the time the AS line was processed, clean_lines already had blank entries. Tighter check: `not any(l.strip() for l in clean_lines)` ("no non-blank body content yet"), and extended to also swallow the leading blanks. Cleaned SQL now starts cleanly at the first real body keyword.]
2026-05-24  commit  69c4b09 preprocess_ssms: skip bare AS line after matched CREATE wrapper  [affected: sql_logic_extractor/resolve.py + parsing_rules/fixtures/strip_ssms_preamble/. State-machine leak: when SSMS exports a view with a long bracketed name like [Reporting].[V_CCHCS_DXP_HP_Mychart_PBI], AS lands on its own line; the CREATE matcher set body_started=True, then the bare AS line fell through to the terminal block and got appended to clean_lines. sqlglot then failed at the next WITH/SELECT with "Required keyword: this missing for class Alias". Fix: narrow check that skips bare AS only when body_started is True AND clean_lines is still empty -- legit AS tokens inside the body unaffected. Fixture expanded to the actual MyChart bug shape so the regression is caught.]
2026-05-24  commit  1a8f588 parsing_rules: strip_ssms_preamble rule -- trim everything before CREATE  [affected: sql_logic_extractor/parsing_rules/rules.py (new rule placed first in PARSING_RULES), fixtures/strip_ssms_preamble/ NEW, sql_logic_extractor/resolve.py (new _extract_object_header helper called from preprocess_ssms before apply_all). Replaces line-by-line keyword whack-a-mole with a single regex that trims everything before the first CREATE [OR ALTER|REPLACE] (VIEW|PROCEDURE|PROC|FUNCTION|TRIGGER). Object metadata (schema/name/script_date) preserved via the new helper; free-form Author/Description comments accepted as lost. 11/11 parsing_rules tests pass.]
2026-05-24  commit  95b8a35 notebooks: add diagnose_one_parse_error -- focused triage for one .sql file  [affected: notebooks/diagnose_one_parse_error.py NEW. Two cells: Cell A diagnoses one specific file with three-stage chain (raw -> preprocess_ssms -> apply_all -> sqlglot.parse with no-dialect + tsql); Cell B loops the same chain over a directory and prints a verdict tally. Built for the MyChart pilot (11 views) which doesn't need the timing_audit CSV pre-step that the older verify_and_diagnose_one_error.py expects.]
2026-05-24  commit  d06f041 operate: add convert_utf16_to_utf8.ps1 for local conversion before upload  [affected: tools/operate/convert_utf16_to_utf8.ps1 NEW. Workaround for two compounding traps: (a) older SSMS Generate Scripts doesn't expose UTF-8 as an encoding option (only Unicode/UTF-16 LE BOM), and (b) Fabric's lakehouse mount silently rejects plain Python writes so in-Fabric conversion doesn't persist even when notebookutils.fs.put returns True. Script walks a folder of .sql files locally on the workstation and rewrites each one UTF-16 -> UTF-8-without-BOM, ready for upload to OneLake. Long-term recommendation in comments: switch to mssql-scripter which defaults to UTF-8.]
2026-05-24  commit  52303df operate: route convert_to_utf8 writes through Fabric fs.put when available  [affected: tools/operate/check_corpus_encoding.py. Plain Python open(file,"w") on the Fabric lakehouse mount silently no-ops. Auto-detect notebookutils (or fallback mssparkutils) at import time; route writes through fs.put when in Fabric, plain Python otherwise. Outside Fabric behavior unchanged. Note: in practice this fix did NOT solve the user's MyChart corpus -- fs.put returns True but writes still don't persist (cache/mount quirk we couldn't diagnose remotely). The local-PowerShell workaround in d06f041 was the actual unblock.]
2026-05-24  commit  ec4a041 operate: add check_corpus_encoding -- triage encoding-induced parse failures  [affected: tools/operate/check_corpus_encoding.py NEW. Diagnoses whether sqlglot parse failures are real T-SQL issues or just SSMS-exported UTF-16 LE being mis-read as UTF-8. BOM-signature detection across all .sql files in a corpus dir, plus readable preview of first N files using the detected encoding, single-line VERDICT bucket, and bonus convert_to_utf8(dry_run=...) helper. Usable as CLI or notebook import. Motivated by 5/11 MyChart views failing the term-extraction parse pass.]
2026-05-23  commit  adc62bf mock v4: signed grain-changers footer (rename from grain-expanders)  [affected: tools/diagnostics/mock_view_matrix.py + docs/mocks/patient_access_view_matrix.md. TABLE_GRAIN gains category+level fields; PAT_PCP gets level=-1 with label "↓ per patient". New _view_grain_change() helper computes signed integer per view: +N for finer-grain join count, -N for anchor levels coarser than cohort, 0 at cohort. Footer renamed grain-changers joined; reads R1=-1 / R2-R5=0 / R6=+2. Asymmetric encoding intentional: counting for compounding finer-grain joins, offset for categorical coarser-cohort anchor. Interpretation reframed: R6 is finer-grain extension (split into measurement-grain model); R1 is different-cohort outlier (patient-grain model) -- same outlier magnitude, different model recommendations.]
2026-05-23  commit  995451e mock v3: add grain column + grain-expander flag to table matrix  [affected: tools/diagnostics/mock_view_matrix.py + docs/mocks/patient_access_view_matrix.md. New TABLE_GRAIN dict classifies each fixture table as dim/cohort/code/grain-expander relative to community cohort (encounter). Table matrix gains `grain` column + `grain-expanders joined` footer; R6 = 2 expanders (FLOWSHEET, PAT_ENC_DX), all others = 0. Decision: grain-expanders still count toward alignment score, AND get flagged separately. Filter and base-column matrices unchanged. Interpretation reframed: table matrix is determining axis; R1 is different-cohort-grain outlier, R6 is finer-grain-extension outlier -- different diagnoses. Production version will read cardinality from Clarity metadata table (user to provide table+column names later).]
2026-05-23  commit  dad33f4 wiki: add Clarity table families concept page  [affected: wiki/concepts/clarity-table-families.md NEW, wiki/index.md adds "Domain knowledge" subsection. First concrete Epic-domain page (prior concepts were governance-philosophy). Captures prefix taxonomy (PAT_*/HSP_*/ORDER_*/FLOWSHEET facts; CLARITY_*/PATIENT conformed dims; ZC_* lookups) + grain quick-reference + the granularity-as-modeling-axis rule that emerged from the matrix design review.]
2026-05-23  commit  c1e4b37 mock v2: 3-matrix feature view with base-column tracing + academic-idea call-outs  [affected: tools/diagnostics/mock_view_matrix.py + docs/mocks/patient_access_view_matrix.md. Three review points addressed: (1) order matrices structural -> filters -> base columns; (2) trace calculated columns to TABLE.COLUMN pairs so R2 (pct_closed_24h) and R3 (close_rate) now show shared PAT_ENC.STATUS_C/ENC_DATE/CLOSE_DATE + ZC_APPT_STATUS; (3) call out academic ideas in action -- frequent itemset mining (R1+R2 filter pair), biclustering (R4+R5 base-col submatrix), workload-driven physical design (dense rows = candidate materialized-view def). New alignment footer (% of dense rows used) quantifies outlier signal: R6=40/0/31, R1=40/0/23, R2-R5=69-100 on base cols. Filter matrix 0% across the board -- fixture has no shared cohort.]
2026-05-17  commit  5a99b20 Phase 3d: views sidebar + legend overlay + separated view-node layout  [affected: tools/p50_present/community_html.py + view_html.py + test_render.py. Three coordinated improvements: (1) legend overlay top-right corner explaining shapes (hexagon=view, dot=table, etc.); (2) clickable views sidebar (left, 260px) on per-community HTMLs only; sidebar items linked bidirectionally to graph node selection; (3) view nodes manually positioned in a left-column at x=-1300 so they no longer overlap with tables. 205/205 tests pass.]
2026-05-17  commit  46e3ad4 Phase 3c: two-way subgraph isolation on node click  [affected: tools/p50_present/community_html.py + view_html.py + test_render.py. Adds inject_subgraph_isolation_script(): custom JS injected before </body> of each pyvis HTML. Clicking any node fades non-neighbors to ~15% opacity. Click empty canvas to restore. Symmetric -- works for clicking view nodes (lights up their tables) and table nodes (lights up the views using them). Idempotent. Applied to render_community_html, render_overview_html, render_view_html. 202/202 tests pass.]
2026-05-17  commit  9ff6f1b Phase 3b: switch to Option B -- view nodes embedded in community HTML  [affected: tools/p50_present/community_html.py (view nodes + select_menu dropdown), tools/p50_present/view_html.py (long-label bug fix), tools/p30_analyze/view_membership.py (new view_to_tables helper), tools/operate/validate_graph_pivot.py (orchestration). User asked for Option B originally (view-nodes-in-community-HTML); Phase 3a built Option A (per-view HTMLs) by mistake. Both now coexist. Per-community HTML now contains hexagon view nodes connected to their tables via dashed edges; pyvis select_menu dropdown lets users pick a view -> highlight its subgraph in place. Long-SQL-as-edge-label bug from bad join_type data also fixed. 200/200 tests pass.]
2026-05-17  commit  6250acc Phase 3a: per-view HTMLs + community membership strength surfacing  [affected: NEW tools/p30_analyze/view_membership.py (compute_view_membership_strength, view_driver_table, classify_views_by_strength), NEW tools/p50_present/view_html.py (render_view_html, view_html_filename), UPDATED tools/p40_synthesize/community_summary.py (Strong/Weak member split), UPDATED tools/p50_present/community_html.py (per-view links in index), UPDATED tools/operate/validate_graph_pivot.py (orchestration of new outputs), FIXED tools/p20_index/graph_builder.py (deterministic from_table from reads_from_tables order). User-driven diagnostic for spotting outlier views: VW_FOO assigned to claims community on the strength of 1 peripheral table while driven by PATIENT now surfaces clearly. 198/198 tests pass (+10 new). End-to-end verified on similarity_demo sample.]
2026-05-17  commit  eec3580 Phase 2e: rewrite validate_graph_pivot module docstring as orchestrator  [affected: tools/operate/validate_graph_pivot.py. Docstring rewritten from "monolithic diagnostic" to "graph-pivot validation orchestrator" -- describes the 11 orchestration steps with the phase folder each calls into. Unused imports (Counter, defaultdict, is_infrastructure_view) cleaned up. Historical note records the Phase 2a-2e decomposition. File now 410 lines (was ~1110 at start of Phase 2). Phase 2 is structurally complete: every pipeline phase has real production code, validate_graph_pivot is a thin orchestrator. 188/188 tests pass.]
2026-05-17  commit  6ff2edf Phase 2d: move synthesis + presentation pieces to their phase folders  [affected: tools/p40_synthesize/community_summary.py NEW (was validate_graph_pivot.write_communities_markdown), tools/p50_present/community_html.py NEW (3 community renderers + helpers + public palette constants, was validate_graph_pivot Section 5). validate_graph_pivot.py down ~380 lines (now ~388 lines total, started Phase 2 at ~1110). 188/188 tests pass. p40_synthesize has 5 real files; p50_present has 2.]
2026-05-17  commit  f5bb561 Phase 2c: carve out p30_analyze (5 analysis modules + tests)  [affected: tools/p30_analyze/{projection,bridges,communities,primary_community,community_analysis}.py NEW. tools/p30_analyze/tests/test_p30_analyze.py NEW (5 TestCase classes, 13 tests). tools/operate/validate_graph_pivot.py: 5 inline analysis functions removed (~250 lines), now imported from p30_analyze. tools/operate/tests/test_validate_graph_pivot.py: 4 analysis test classes removed (moved to p30_analyze/tests). 188/188 tests pass. The analytical heart of the GOVERN layer is now in its proper home -- p30_analyze has REAL CONTENT.]
2026-05-17  commit  da11c5e Phase 2b: promote build_graph from validate_graph_pivot to p20_index/graph_builder.py  [affected: tools/p20_index/graph_builder.py completely rewritten (old 3-entry-point graph_explore-era API deleted, new production build_graph + build_corpus_graph from validate_graph_pivot promoted). tools/operate/validate_graph_pivot.py removed ~235 lines (Section 2 Graph construction). Tests reorganized: TestGraphConstruction moved from operate/tests to p20_index/tests (where build_graph now lives). p50_present/tests/test_render.py updated to use build_graph([view]) instead of deleted build_view_graph(view). 182/182 tests pass. p20_index has its real production code now -- graph_builder.py + term_extraction.py form the structural + lexical layer the brainstorm called for.]
2026-05-17  commit  10a2d8a Phase 2a: extract corpus IO + table-name helpers + view filter into shared/  [affected: tools/shared/corpus_io.py (NEW, load_corpus moved from operate/validate_graph_pivot), tools/shared/table_names.py (NEW, bare_table_name + is_zc_table + is_cte_or_scope_reference, underscore prefix dropped since they're now public), tools/shared/view_filter.py (NEW, is_infrastructure_view + filter_business_views + DEFAULT_INFRASTRUCTURE_PATTERNS), three new test files in shared/tests/, operate/validate_graph_pivot.py imports from shared/. First commit of Phase 2 -- decomposing the validation diagnostic into production pipeline phases. 126/126 tests pass.]
2026-05-17  commit  40102e9 Phase 1m: document the deliverables map in ARCHITECTURE.md  [affected: ARCHITECTURE.md adds "What this delivers" section organizing outputs by audience and purpose: three layers (CATALOG / GOVERN / VISUALIZE) + operate sidecar, with concrete artifact list and audience per layer. Caps off Phase 1 of the restructure (Phase 0 + Phases 1a-1m, ~14 commits total). Ratification framing clarified: stewards ratify in Collibra, not in this codebase.]
2026-05-17  commit  da6f3e4 Phase 1l: drop p60_hitl + p70_feedback (Collibra owns ratification)  [affected: tools/p60_hitl/ and tools/p70_feedback/ skeleton folders DELETED (no content was ever written there); ARCHITECTURE.md and tools/PHASES.md updated from 7-phase to 5-phase pipeline; layer labels (CATALOG / GOVERN / VISUALIZE) added to phase diagram; p20/p30/p40 __init__.py docstrings cleaned of p70_feedback mentions. Sharper product: we are the evidence-pack generator that makes Collibra usable, not a competitor.]
2026-05-17  commit  7c5bcba gitignore my_notes/ -- untrack local sample corpora accidentally committed in 3d9dbb3  [affected: .gitignore (added my_notes/), my_notes/* (untracked from git, still on local disk). 39 files removed from git tracking. Files remain in commit 3d9dbb3 in history; no force-push.]
2026-05-17  commit  3d9dbb3 Phase 1k: consolidate ops -- rename diagnostics->operate, absorb parser-dev tools + inventory_manifest  [affected: tools/operate/ created from rename of tools/diagnostics/; absorbs preflight_check, auto_propose_rule (+ hypotheses), timing_audit, diagnose_parse_failure.py, AND inventory_manifest (moved from p40_synthesize since its audience is BI devs running the system, not stewards). Folder name reflects the broader scope of operations work (parser dev + admin), not just diagnosis. All importers updated across operate/, docs, notebooks. 104/104 phase+shared+operate tests pass. NOTE: this commit also accidentally included my_notes/ files (user's local sample corpora); needs cleanup commit to gitignore them.]
2026-05-17  commit  1e365b2 Phase 1j: delete tools/comment_audit/ (one-off analysis, findings absorbed)  [affected: tools/comment_audit/ entire folder deleted (189 lines + tests). No live importers; findings were absorbed into Tools 3/4 during their original development. Git history preserves.]
2026-05-17  commit  57bb7ca Phase 1i: migrate tools/dataset_extract/ -> tools/p40_synthesize/dataset_extract.py + dataset_render.py  [affected: tools/p40_synthesize/dataset_extract.py (was dataset_extract/batch.py), tools/p40_synthesize/dataset_render.py (was dataset_extract/render.py). Same pattern as cohort_extract: flat files with shared prefix. 43/43 p40_synthesize tests + 16/16 diagnostic tests green.]
2026-05-17  commit  194b013 Phase 1h: migrate tools/term_extraction/ -> tools/p20_index/term_extraction.py  [affected: tools/p20_index/term_extraction.py (formerly term_extraction/batch.py, renamed for descriptive consistency at phase-folder level), tools/p20_index/tests/test_term_extraction.py (renamed from test_batch.py), docs/running_phase_d_tools.md (3 module-path updates), tools/PHASES.md migration table marked Done. p20_index now has structural (graph_builder.py) AND lexical (term_extraction.py) layers as intentional partners. 70/70 phase+shared tests + 16/16 diagnostic tests green.]
2026-05-17  commit  449164e Phase 1g: split tools/similar_logic_grouper/ -> fingerprint to shared/, batch.py deleted  [affected: tools/shared/fingerprint.py (moved from similar_logic_grouper, actively used by p10_extract for column fingerprints), tools/shared/tests/test_fingerprint.py + __init__.py, tools/similar_logic_grouper/{__init__,batch}.py DELETED (152 lines superseded; p30_analyze will produce same finding from unified graph). p10_extract/batch.py import updated. PHASES.md migration table marked Done. shared/ now has 2 real files (dim_filter, fingerprint). 59/59 phase+shared tests + 16/16 diagnostic tests green.]
2026-05-17  commit  79e5f8e Phase 1f: delete tools/similarity/ (superseded by graph pivot)  [affected: tools/similarity/ entire folder DELETED (batch.py 230 lines, clusters.py 279 lines, signatures.py 376 lines, plus tests; 909 lines total). tools/diagnostics/check_similarity.py also deleted (only consumer of similarity, its purpose was triaging similarity-tool output). Graph approach in validate_graph_pivot.py supersedes it -- validation returned PASS on real 130-view corpus. Git history preserves the deleted code. tools/PHASES.md migration table updated. 60/60 phase tests + 16/16 diagnostic tests green.]
2026-05-17  commit  e3d16d7 Phase 1e: split tools/view_shape_compare/ -> dim_filter to shared/, rest deleted  [affected: tools/shared/dim_filter.py (moved from view_shape_compare), tools/view_shape_compare/{__init__,batch,clusters,features,tests/__init__,tests/test_view_shape_compare}.py (all DELETED, git history preserves them at parent commit), tools/p40_synthesize/cohort_extract.py + tests (import path updated), docs/running_phase_d_tools.md (Compare view shapes section replaced with pointer to validate_graph_pivot), tools/PHASES.md migration table marked Done for this row. tools/shared/ now has its first real content. 35/35 + 16/16 tests pass.]
2026-05-17  commit  4ac0bd9 Phase 1d: migrate tools/cohort_extract/ -> tools/p40_synthesize/cohort_extract.py + cohort_render.py  [affected: tools/p40_synthesize/cohort_extract.py (was cohort_extract/batch.py), tools/p40_synthesize/cohort_render.py (was cohort_extract/render.py), tools/p40_synthesize/tests/test_cohort_extract.py, docs/running_phase_d_tools.md. Two coupled files become two flat files with shared cohort_ prefix; relative import `from .render` -> `from .cohort_render`. view_shape_compare.dim_filter dependency preserved at legacy path -- migrates later. 35/35 p40_synthesize tests + 16/16 diagnostic tests green. p40_synthesize now has 3 real files.]
2026-05-17  commit  73bab35 Phase 1c: migrate tools/inventory_manifest/ -> tools/p40_synthesize/inventory_manifest.py  [affected: tools/p40_synthesize/inventory_manifest.py (formerly inventory_manifest/batch.py), tools/p40_synthesize/tests/test_inventory_manifest.py. Flat-file structure within phase folders: each tool becomes one .py file named for itself, not a sub-package. Zero external importers; cleanest migration so far. First real content for p40_synthesize. 7/7 tool tests + 16/16 diagnostic tests green.]
2026-05-17  commit  112e21f Phase 1b: split tools/graph_explore/ into p20_index/ + p50_present/  [affected: tools/p20_index/graph_builder.py (formerly graph_explore/build.py), tools/p50_present/render.py (formerly graph_explore/render.py). Two concerns crammed into one folder finally split where they belong in the new phase structure. Tests split too: TestBuild -> p20_index/tests/test_graph_builder.py (5 tests), TestRender -> p50_present/tests/test_render.py (2 tests). Each test file is self-contained. graph_explore/ folder fully removed (no external importers). All 7 split tests + 16 diagnostic tests green.]
2026-05-17  commit  261003c Phase 1a: migrate tools/extract_corpus/ -> tools/p10_extract/  [affected: tools/p10_extract/ (renamed from extract_corpus), 5 downstream test importers, docs/running_phase_d_tools.md. First actual code-move of the restructure; git tracks all moves as renames (preserves history). No behavioral changes. 16/16 validate_graph_pivot + 18/18 p10_extract + 67/67 downstream tests all green.]
2026-05-17  commit  1e3ec52 Phase 0: codebase restructure skeleton + ARCHITECTURE.md + tools/PHASES.md  [affected: ARCHITECTURE.md (new, repo root), tools/PHASES.md (new), tools/p10_extract/p20_index/p30_analyze/p40_synthesize/p50_present/p60_hitl/p70_feedback/shared/ (new skeleton folders with __init__.py docstrings). Zero code moves -- nothing in existing imports breaks. Documents the pN0_ phase-prefix convention, the seven-phase pipeline (extract -> index -> analyze -> synthesize -> present -> hitl -> feedback), the migration table mapping legacy tools to new homes. User decided to restructure in place rather than start a new repo; this is the first step.]
2026-05-17  commit  c1ce79e validate_graph_pivot: bridge detection, infra-view filter, primary-community, per-community HTMLs  [affected: tools/diagnostics/validate_graph_pivot.py + tests; refinements after the first real 130-view run came back PASS but with three noise issues. (1) Auto bridge detection by degree percentile -- no hardcoded dimension list, the graph reveals PATIENT/CLARITY_SER/CLARITY_DEP empirically as high-degree hubs and excludes them from community detection. (2) Infrastructure-view filter for collibra/metadata/catalog/ingest names + sys/INFORMATION_SCHEMA sources, CLI-overridable. (3) Each view assigned to ONE primary community (most-tables-wins); cross-domain spanners reported separately as their own steward finding. (4) Per-community HTMLs at communities/community_NN_<top-table>.html with index.html, bridges shown muted; auto-physics-off above 200 nodes. 16 tests, all green.]
2026-05-16  commit  4c17090 diagnostics: validate_graph_pivot -- test the graph-pivot architecture hypothesis  [affected: tools/diagnostics/validate_graph_pivot.py (new); built during the Q1-Q3 architecture brainstorm to validate whether the graph pivot (corpus -> unified networkx graph with View/Scope/Table/Column nodes, view-provenance on edges) is justified before committing the codebase to it. Runs Louvain on an undirected weighted table-projection (edge weight = scope-level co-occurrence) and emits graph.html (interactive, CDN-free, offline-safe), communities.md (per-community summary), and validation_report.md (PASS / INCONCLUSIVE / REVIEW NEEDED verdict). CTE references in joins[].right_table strip the cte: prefix, so we collect scope names per view and exclude joins targeting a known scope from the table set. Heavily commented per the user's Python-learning preference. 10 unit tests; smoke-tested against bi_complex_sample (1 view, 12 tables, 4 communities) and similarity_demo (2 views, 26 tables, 6 communities). Decision point: user will run this against the 130-view Fabric corpus to determine whether to commit to the full graph-pivot codebase restructure.]
2026-05-03  commit  5f2595a Tool 17: graph_explore -- corpus.jsonl -> networkx + interactive HTML  [affected: tools/graph_explore/ (new); build_view_graph / build_cluster_graph / build_corpus_graph turn ViewV1 dicts into typed-node MultiDiGraphs (view/scope/column/table/filter); tables are GLOBAL nodes so cross-view connections through shared subjects (PATIENT, COVERAGE) are visible. Renderers: render_pyvis (interactive HTML), export_graphml (Gephi), render_inline (matplotlib). Lazy imports keep the module cheap. 7 unit tests; smoke-tested against my_notes/similarity_demo/corpus.jsonl producing 142 nodes / 258 edges across 2 views. Built after user discussion about whether graphs would expose similarity better than tabular clusters -- this is the in-notebook approximation since Fabric does not have a native graph DB.]
2026-05-09  commit  diagnostics: check_similarity -- triage unexpectedly large/small clusters  [affected: tools/diagnostics/check_similarity.py (new); two-stage check: (1) verify the latest similarity code is loaded (alias-stripping + strict L1) by inspecting source on disk; (2) report driver distribution across the corpus -- distinguishes "one driver genuinely dominates" (not a bug) from "drivers are diverse but L1 is still one cluster" (real bug)]
2026-05-09  commit  3ac3a7c similarity: L1 = strict driver equality + alias-agnostic filter canonicalization  [affected: tools/similarity/{signatures,clusters}.py; user reported 1 L1 cluster of 120 views with mostly-singleton L2+ sub-clusters. Two fixes: (1) revert L1 to strict driver equality (matches user's original spec; the looser one-way containment caused transitive over-clustering in healthcare corpora). (2) Strip table-alias prefixes during filter canonicalization so `CVG.COVERAGE_TYPE_C = 2` and `C.COVERAGE_TYPE_C = 2` produce the same canonical string -- real-world views from different developers use different aliases.]
2026-05-09  commit  e40ebab Tool 16: similarity -- 4-level view clustering for goldmine governance  [affected: tools/similarity/ (new); reads corpus.jsonl, builds per-view signatures (leaf driver chased through CTEs, type-agnostic joined_set with consistency annotation, transitively-resolved projections, canonical filters with equi-keys stripped), clusters at L1/L2/L3/L4 via union-find on driver containment + groupby on stricter axes; outputs clusters_L<N>.json/.md per level + features.json; 11 tests including the three dry-run shapes from the design discussion]
2026-05-09  commit  0e0d0e7 Robust ZC lookup: column-name-keyed CSV + dynamic code col discovery  [affected: data/schemas/extract_zc_values.sql now derives the actual `_C` column per ZC table from CLARITY_COL (instead of assuming ZC_<X>↔<X>_C); CSV gains a column_name field; extract_corpus indexes by (column_name, code) primarily, fallback to (zc_table, code) via naming convention for old CSVs. Fixes ZC_CLM_AP_STAT-style tables whose code col is AP_STS_C, not CLM_AP_STAT_C.]
2026-05-08  commit  e460dd9 inventory_manifest: also infer ZC tables from <X>_C column references  [affected: tools/inventory_manifest/batch.py; previously used_zc_tables only contained ZC tables explicitly JOINed/FROM'd. But Clarity views often filter on <X>_C codes WITHOUT joining ZC_<X> -- those ZC tables were missed in the SQL extract. Now also infers ZC_<X> from any <X>_C column reference. bi_complex went from 0 to 3 ZC tables in its manifest as a result.]
2026-05-08  commit  8f8954d diagnostics: add probe_zc_filter to inspect a specific corpus filter  [affected: tools/diagnostics/check_zc_lookups.py; new function takes substring/code criteria and dumps the first matching filter's expression/english/lookups so users can paste it alongside cohorts.md to diagnose missing annotations; CLI gains --probe-zc-table / --probe-code / --probe-max args]
2026-05-08  commit  e92bb88 ZC lookups: extend to IN(), reversed EQ, and != predicates + IN-list annotation  [affected: extract_corpus walks AST for exp.In and exp.NEQ in addition to exp.EQ, with reversed-order matching for column-on-RHS cases; cohort renderer annotation widened to match `<code>` preceded by `=`, `,`, or `(` so IN-list values get inline /* name */ tags. User reported only 18 of 929 filters resolved with EQ-only -- IN was the dominant pattern in their corpus.]
2026-05-07  commit  fed4502 tools/diagnostics/check_zc_lookups -- triage missing /* name */ annotations  [affected: tools/diagnostics/ (new); two-stage check: total zc_lookups embedded in corpus.jsonl, then zc_values.csv header validity (`zc_table,code,name` lowercase). Common SSMS gotcha: uppercase headers or missing header line silently load zero rows.]
2026-05-07  commit  3a3650c Tool 15: inventory_manifest -- narrow Clarity / ZC extracts to used tables  [affected: tools/inventory_manifest/ (new); reads corpus.jsonl, emits dedupe'd used_tables.txt, used_zc_tables.txt, used_columns.csv, plus paste-ready VALUES clauses for the SSMS extracts; CTE/derived aliases filtered out so they don't leak into the table list; extract_zc_values.sql now supports a temp-table @TableFilter for 10-100x faster execution]
2026-05-07  commit  1467372 extract_zc_values.sql: switch from sys.tables/sys.columns to CLARITY_TBL/CLARITY_COL  [affected: data/schemas/extract_zc_values.sql; user reported the sys.* version returned no results in their Clarity env; switched to the Clarity metadata pattern (CLARITY.dbo.CLARITY_TBL/CLARITY_COL) used by extract_clarity_metadata.sql, with naming-convention-driven code column derivation (ZC_X -> X_C); added PRINT debug aids in Messages tab]
2026-05-07  commit  db5dac1 Add data/schemas/extract_zc_values.sql -- the SSMS query to populate zc_values.csv  [affected: data/schemas/extract_zc_values.sql; auto-discovers all ZC_* tables in the connected Clarity DB, unions (zc_table, code, name) tuples via dynamic SQL; documented usage notes including multi-DB instructions]
2026-05-07  commit  ae9c1d1 ZC code-to-name lookups in filters via zc_values.csv  [affected: corpus_schema ZcLookupV1 + FilterV1.zc_lookups additive; extract_corpus loads data/dictionaries/zc_values.csv (zc_table,code,name) and walks filter ASTs for <X>_C = <N> references resolving to ZcLookupV1 entries; cohort renderer annotates filter text with /* name */ inline; user supplies the CSV via SQL extraction at work]
2026-05-07  commit  1e5b45d Capture inline filter comments as structured field for future semantic extraction  [affected: corpus_schema FilterV1.inline_comments additive field; extract_corpus splits /* */ and -- comments out of filter expressions, surfaces them as a list per filter; cohort rendering shows clean SQL; comments preserved in corpus.jsonl for downstream tools that want STATUS_C=2/*Managed Care*/ business-meaning hints]
2026-05-07  commit  2051c26 cohort_extract: JOIN ON filters render with TABLE(alias).column form  [affected: none — code; for kind=join_on filters, output now uses the SQL form with <TABLE>(<alias>).<col> alias expansion (e.g., CVG_SUBSCR_ADDR(CSA).LINE = 1) instead of the alias-stripped English; equi-keys still dropped at SQL level; alias map sourced from scope.joins[*].right_alias/right_table]
2026-05-07  commit  b6745a8 cohort_extract: keep JOIN ON business filters, strip equi-join keys  [affected: none — code; reverses prior commit's blanket drop of join_on; now extracts the business predicates within JOIN clauses while dropping pure key fragments like `<X> = <X>` (with both sides bare word phrases); v_active_members CTE recovered 5 real filters that were lost in the previous version]
2026-05-07  commit  9599910 cohort_extract: drop JOIN ON predicates from cohort filters  [affected: none — code; only WHERE/HAVING/QUALIFY filter kinds emitted in cohort.filters; join_on / exists / in linkages are structural not population-defining; v_active_members CTE filters dropped from 6 long JOIN predicates to (none)]
2026-05-06  commit  e47cb87 cohort_extract: pull table descriptions from clarity_schema.json (single source of truth)  [affected: data/schemas/csv_to_schema.py reads TABLE_SHORT_DESCRIPTION column; cohort_extract loads from schema JSON, YAML becomes secondary overlay for tables not in Clarity (custom views/fact tables); user adds TABLE_SHORT_DESCRIPTION column to clarity_metadata.csv at work]
2026-05-06  commit  2346016 cohort_extract Layer 1: head + leaf heuristic (head from FROM driver, 0/1/2+ rule)  [affected: none — code; cohort phrase = head (FROM driver if it contributes a column, else first selected source); 0 others -> head only; 1 other -> "head with other"; 2+ others -> head only (avoid arbitrary pick); v_active_members CTE went from 8-table list to "coverages"; future Layer 2 = per-view -- @cohort: override; Layer 3 = ROW_NUMBER pattern detection]
2026-05-06  commit  6e83145 cohort_extract: cohort tables come from SELECTED columns, not all joins  [affected: none — code; rule pivot per user spec — JOIN-only tables (joined for filter context but not projected) are excluded from the cohort phrase; ZC_* lookup tables are excluded even when projected (they're labels not entities); CLARITY_EDG added to YAML for "patient ICD10 codes" example; 3 new tests covering the pivot]
2026-05-06  commit  f9af0cc Tool 14: cohort_extract — population-level governance rendering  [affected: docs/running_phase_d_tools.md; new tool reads corpus.jsonl and renders each scope as cohort + filters (skips column docs); table_short_descriptions.yaml seeded with healthcare table mappings; dim filter still applied but kept-when-only-tables; outputs cohorts.md + cohorts.json]
2026-05-04  commit  e65463a Translator: short_description-only fallback + alias inlining for passthroughs  [affected: none — code; column translator no longer uses Clarity's verbose DESCRIPTION; falls back to column-name abbreviation expansion when short_description is absent. Plus: _resolve_column_local now inlines aliases for single-source passthroughs so ZSR.NAME -> ZC_SUBSC_RACE.NAME -> "Subscriber Race"; goldens re-baselined]
2026-05-04  commit  453d850 Tool 13: dataset_extract — render scopes as datasets (JSON + MD)  [affected: docs/running_phase_d_tools.md; new tool reads corpus.jsonl and renders each scope as a dataset with name (from CTE alias), base_datasets (lineage), data_columns (English), filters (English); two outputs datasets.md + datasets.json]
2026-05-04  commit  aacfa66 Column translator: short_description preference + ZC.NAME heuristic + Clarity abbreviations  [affected: none — code; ZC_<X>.NAME projections now translate to the table domain (e.g., ZC_TAX_STATE.NAME → "Tax State"); short_description column on schema entries preferred over Clarity DESCRIPTION; csv_to_schema reads SHORT_DESCRIPTION from CSV; SUBSC/CVG/BEN/EPP/EPT abbreviations added; goldens re-baselined]
2026-05-04  commit  32e70e6 view_shape_compare: JSON pair-diff output + multi-scope (CTE) aggregation  [affected: docs/running_phase_d_tools.md; pairs.json with side-by-side shared/only_a/only_b breakdown per axis + per-scope decomposition; CTE-internal fact tables now correctly aggregated up to the view level (main-only bug fixed); CSV outputs replaced with JSON]
2026-05-03  commit  2312f2c Tool 12: view_shape_compare (table+join similarity, dim-aware clustering)  [affected: docs/running_phase_d_tools.md; new tool reads corpus.jsonl, clusters structurally-identical views, emits cross-cluster pair flags for fact_subset/overlap/dim_extension/same_facts_different_joins; data/dictionaries/dim_tables.txt seeded with PATIENT, ZC_*, CLARITY_*]
2026-05-03  commit  adcb4a7 Schema additive: JoinV1 on ScopeV1  [affected: none — code; structured (right_table, join_type, on_expression) on each scope; resolver populates ScopedJoin; extract_corpus mirrors as JoinV1; powers upcoming view-shape comparison]
2026-05-03  commit  b4f576d docs/running_phase_d_tools.md cell 4 fix: BOM-aware read + preprocess_ssms in standalone smoke check  [affected: docs/running_phase_d_tools.md; users hit utf-8 0xff decode error on SSMS UTF-16 exports]
2026-05-03  commit  339a7b7 Phase D fix: preprocess SSMS script boilerplate before parsing  [affected: none — code; extract_corpus and term_extraction call preprocess_ssms first; surfaces error type on per-view progress line; regression test for SSMS export format]
2026-05-03  commit  6e86e9d Add docs/running_phase_d_tools.md (notebook + CLI cells for extract_corpus and term_extraction)  [affected: none — operational doc; copy-paste cells for Fabric users to run scope-correct corpus extraction]
2026-05-03  commit  8cd2ddb Phase D Commit 3: scope-correct standalone term extraction  [affected: none — code; tools/term_extraction/batch.py uses resolve_all_scoped, default emits main-scope only, --all-scopes flag for intermediate; CTE filters no longer pollute Term.filters]
2026-05-03  commit  442b4a7 Phase D Commit 2: corpus schema v3 (scope tree) + extractor rewrite  [affected: none — code; ScopeV1/FilterV1 nested tree replaces v2 flat form; extract_corpus walks ResolvedScopeTree; v1/v2 corpus.jsonl no longer readable]
2026-05-03  commit  855d19f Phase D Commit 1: scope-correct resolver (resolve_all_scoped, additive)  [affected: none — code; new ResolvedScope* dataclasses + LineageResolver.resolve_all_scoped() with no cross-scope filter propagation, fixes false-grouping risk in similar_logic_grouper]
2026-05-02  commit  649a118 Layer 4: AST-level rule registry + combined parse pipeline  [affected: none — code; AstRule dataclass + drop_table_hints starter rule + parse_with_rules entry point]
2026-05-02  commit  7d27955 Comment-as-data steps 2-4: bind comments to AST + surface in Tool 3/4  [affected: none — code; Tool 4 business_description leads with author voice; Tool 3 column rows expose author_notes]
2026-05-02  commit  1b19d2e Comment-as-data step 1: extractor + Comment entity + corpus audit  [affected: none — code; lays groundwork for surfacing author comments in Tool 3/4 English output]
2026-05-02  commit  0c0194e Layer 3: auto-propose parsing rules for unknown_failure views  [affected: none — code; closes the parsing-strategy redesign with hypothesis sweep + token isolation -> markdown proposals]
2026-05-02  commit  f7818cd Layer 2: preflight check -- triage parse health before running tools  [affected: none — code; new tool emits clean / needs_rule / unknown_failure CSV]
2026-05-02  commit  f51ad59 Layer 1: parsing-rule registry replaces inline regex strips  [affected: none — code; declarative Rule dataclasses + fixture-driven tests; 188 tests pass]
2026-05-02  commit  cbf8339 Add Tool 5 (similar_logic_grouper) + full-corpus metadata helper  [affected: none — code; AST fingerprinting for cross-view definition dedup; SSMS @Tables/@Columns generator]
2026-05-02  commit  c209065 Tool 4: format technical and business descriptions as paragraphs  [affected: none — code; newline-separated bullets so cells render as readable text]
2026-05-02  commit  95a877a Tool 4 Phase 2: naturalize engineered business_description prose  [affected: none — code; comment promotion + effective-date folding + _YN normalization + keyword-adjective promotion]
2026-05-02  commit  95eb7a9 Tool 4: rename query_summary -> technical_description, add business_description  [affected: none — code; engineered business prose strips technical noise via pattern-library + post-processing]
2026-05-02  commit  6bc4d1a Emit ALL filter predicates verbatim in query_summary  [affected: none — code; engineered Tool 4 summary now keeps the full business slice for no-LLM use]
2026-05-02  commit  abd6e69 Drop view_file from Tools 1, 2, 4 outputs (parallel to Tool 3 trim)  [affected: none — code; goldens re-baselined]
2026-05-02  commit  c188568 Slim Tool 3 CSV: drop view_file, base_columns, base_tables  [affected: none — code; goldens re-baselined]
2026-05-02  commit  a9e43d6 ASCII-sanitize csv_to_schema.py for Windows-browser uploads  [affected: none — code; em-dash mangling crashed Python's UTF-8 source loader]
2026-05-02  commit  e76d51d Auto-detect CSV encoding in csv_to_schema.py  [affected: none — code; SSMS exports as UTF-16 LE]
2026-05-02  commit  5d30682 Add Fabric notebook script for the all-tools batch runner  [affected: none — code; six cells: deps, repo upload, sys.path, batch_all, peek, LLM]
2026-05-02  commit  f14049b Hard-code @Columns list for V_ACTIVE_MEMBERS + bi_complex  [affected: none — code; pre-populated metadata extract for SSMS run]
2026-05-01  commit  e7cfe62 Add optional @Columns filter to Clarity metadata extract  [affected: none — code; narrows to columns actually referenced per Tool 1 manifest]
2026-05-01  commit  e7fa61e Add sys.* fallback layer to Clarity metadata extract  [affected: none — code; Layer B captures custom V_CCHP_* views that aren't in CLARITY_TBL]
2026-04-29  decide  Pivot away from AIVIA platform; 4-tool product line is the commercialization vehicle, with the day-job SSIS-to-Fabric migration as Use Case #1  [affected: planning/monthly/2026-06,07,08.md, planning/weekly/2026-06-01,08-10,08-24.md, planning/daily/2026-04-28.md, view-migration/REQUIREMENTS.md, wiki/decisions/2026-04-19-*.md, wiki/concepts/article-template.md]
2026-04-28  commit  d445008 Add view-migration tooling for SSIS-to-Fabric workstream  [affected: none — new isolated workstream under view-migration/; separate from extractor commercialization]
2026-04-27  commit  94df49f Move CLI scripts to cli/, lift filters to query level, plumb LLM filter context  [affected: none — code; reorganization + L4 filter consolidation + LLM summary semantic upgrade]
2026-04-20  commit  38c50e9 Add INI-Item-aware schema pipeline (CSV → JSON → translator)  [affected: none — code; operationalizes app-config-id-as-coordination-key.md]
2026-04-20  commit  eeb6605 Rewire offline_translate.py on top of the recursive pattern library  [affected: none — code; archives legacy version for reference]
2026-04-20  commit  f26fad2 Add golden tests for recursive offline translator (Step 4)  [affected: none — tests + fixtures]
2026-04-20  commit  cfb300f Implement recursive walker + pattern library (Steps 2+3)  [affected: none — code]
2026-04-20  commit  5e082a4 Scaffold pattern library data model (Step 1)  [affected: none — code]
2026-04-20  commit  bae3727 Adopt recursive translation principle for offline_translate  [affected: recursive-translation-principle, 2026-04-20-adopt-recursive-translation, index]
2026-04-20  decide  Added `recursive-translation-principle` (concept) and `2026-04-20-adopt-recursive-translation` (decision) capturing the architectural pivot for offline_translate.py: recursion to raw-column base case + growing pattern library + unknown patterns/columns as first-class governance signals. Test findings from 11-query run embedded as motivating context.  [affected: recursive-translation-principle, 2026-04-20-adopt-recursive-translation, index]
2026-04-20  commit  9347be5 Log Shipment 1 to work (extract + normalize + resolve + compare_lineage)  [affected: none — work-shipments ledger, repo root]
2026-04-19  commit  5d17bcd Add project instructions, graphify outputs, and Claude Code hook config  [affected: none — project infra]
2026-04-19  commit  4a30ee0 Adopt Thinking in Public strategy and publishing pipeline  [affected: 2026-04-19-thinking-in-public-strategy, 2026-04-19-publishing-automation-architecture, article-template, index, log]
2026-04-19  decide  Adopted Thinking in Public (TIP) as publishing posture and semi-automated three-tier pipeline as publishing architecture. Added `article-template.md` (reusable 8-section structure). Resolved the healthcare-rich-wiki vs. industry-agnostic-public tension via two-layer separation (wiki stays private source; public articles are translations).  [affected: 2026-04-19-thinking-in-public-strategy, 2026-04-19-publishing-automation-architecture, article-template, index]
2026-04-19  commit  c07df14 Seed conceptual wiki with governance-forks framework  [affected: all new concept pages + decision + index + log]
2026-04-19  ingest  Added `app-config-id-as-coordination-key.md` — Yang's Epic INI-Item insight captured. This is the operational primitive that closes the app-team coordination loop: app analysts already identify the INI-Item as part of their change workflow, so lookup-before-change piggybacks on an existing step with no new process. Clarity pre-packages the catalog, extractor supplies the cross-reference. Identified as a go-to-market wedge for healthcare Epic shops — a day-one valuable connector feature no competitor has. Cross-linked from app-teams fork and silent-upstream-break scene (added "how it would have gone differently" section).  [affected: app-config-id-as-coordination-key, app-teams-in-dg-vs-out, silent-upstream-break, index]
2026-04-19  ingest  Added Fork 8 (app teams in DG, or out) with fork page and canonical "silent upstream break" scene (second-class companion to Patient A01). Cross-linked `govern-authored-meaning.md` with the upstream-extension implication. Establishes DG-as-enterprise vs. DG-as-analytics distinction; honest third path is coordination-minimal, approval-narrow. Updated forks catalog and index.  [affected: app-teams-in-dg-vs-out, silent-upstream-break, govern-authored-meaning, governance-forks-catalog, index]
2026-04-19  ingest  Added Fork 7 (catch-up vs. spec-first) with the fork page, the "definitions are emergent" counterweight concept, and the "spec-first as data contract" intellectual anchor. Establishes site's non-obvious position: neither pure posture is correct; extractor-enabled selective promotion is the third path. Updated forks catalog and index.  [affected: catch-up-vs-spec-first, definitions-are-emergent, spec-first-as-data-contract, governance-forks-catalog, index]
2026-04-19  ingest  Added Fork 6 (catalog-first vs. governance-first) with three supporting pages: the fork itself, "catalog is not governance" foundational frame, and the automation-asymmetry insight that reframes extractor's category as the first scalable governance-authoring primitive. Updated forks catalog and index.  [affected: catalog-first-vs-governance-first, catalog-is-not-governance, catalog-vs-governance-automation-asymmetry, governance-forks-catalog, index]
2026-04-19  ingest  Seeded 6 concept pages + 1 decision page from live brainstorm on content-site structure (fork model), data-vs-metadata ownership, governance-vs-compliance layer split, authored-meaning governance, Patient A01 canonical scene, SQL as definitional moments, fork catalog working draft.  [affected: data-vs-metadata-ownership, governance-vs-compliance-layers, govern-authored-meaning, sql-as-definitional-moments, patient-a01-scene, governance-forks-catalog, 2026-04-19-fork-model-for-content-site]
2026-04-17  init  Wiki seeded: SCHEMA.md, index.md, empty concepts/ and decisions/. Graphify code wiki at graphify-out/wiki/ (21 articles).
